-- Muscle Legends Hub (with Killer tab + Pre‑teleport Anchor & Attachment Management + Torso Size)
-- All UI elements stored in a single 'ui' table to avoid local register overflow.
-- ==================== SAFE CLEANUP ====================
local oldUI = getgenv().ML_Expert_UI
local oldLoops = getgenv().ML_Expert_Loops
getgenv().ML_Expert_UI = nil
getgenv().ML_Expert_Loops = {}
if oldUI then pcall(function() oldUI:Destroy() end) end
if oldLoops then
for _, loop in ipairs(oldLoops) do
if type(loop) == "thread" then
pcall(task.cancel, loop)
elseif type(loop) == "table" and loop.Disconnect then
pcall(loop.Disconnect, loop)
end
end
end
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")
local VirtualInputManager = game:GetService("VirtualInputManager")
local Stats = game:GetService("Stats")
local player = Players.LocalPlayer
local isMobile = UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled
-- ==================== CONFIG ====================
local Config = { SizeValue = "1", RebirthTarget = "0", WalkSpeed = "16", FlySpeed = "50" }
local function SaveConfig()
pcall(function() writefile("ML_Expert_Config.json", HttpService:JSONEncode(Config)) end)
end
pcall(function()
if isfile("ML_Expert_Config.json") then
local data = HttpService:JSONDecode(readfile("ML_Expert_Config.json"))
Config.SizeValue = data.SizeValue or "1"
Config.RebirthTarget = data.RebirthTarget or "0"
Config.WalkSpeed = data.WalkSpeed or "16"
Config.FlySpeed = data.FlySpeed or "50"
end
end)
-- Disconnect Logger
pcall(function()
game:GetService("GuiService").ErrorMessageChanged:Connect(function(msg)
if msg and msg ~= "" then
pcall(function()
SaveConfig()
local timeFormatted = os.date("%H-%M-%S")
local dateFormatted = os.date("%d-%m-%Y")
local fileName = "DISCONNECTED:" .. timeFormatted .. ", " .. dateFormatted .. ".txt"
local logContent = "--- DISCONNECT LOG ---\nTime: " .. os.date("%c") .. "\nFull Reason/Error Code: " .. msg
writefile(fileName, logContent)
end)
end
end)
end)
-- ==================== STATE VARIABLES ====================
local states = {
selectedRock = nil, selectedMethod = nil, autoGlitch = false, autoGlitchTrain = false,
glitchStartTime = 0, glitchAccumulated = 0, preGlitchPos = nil, isTransitioning = false,
autoLiftNoAnim = false, autoLiftValue = false, autoRebirthTarget = false, autoRebirthInf = false,
selectedTool = nil, autoEquip = false, autoClaim = false, autoPunch = false, accelReps = false,
fastPunch = false, autoSize = false, godMode = false, lockedPos = nil, lastSafePos = nil,
autoWalkSpeed = false, fly = false, flyBody = nil, autoMuscleKing = false,
statsTarget = nil, trackStats = false,
viewing = false, withoutFormatNum = false, autoFortuneWheel = false,
currentPetLabel = nil, currentPetTable = nil, currentAuraLabel = nil, currentAuraTable = nil,
lessLag = false,
selectedMap = nil, selectedGym = nil, autoGym = false, autoGymThread = nil,
selectedPetCrystal = nil, selectedPetCrystalData = nil,
selectedAuraCrystal = nil, selectedAuraCrystalData = nil,
autoPetBuy = false, autoPetBuyThread = nil,
autoAuraBuy = false, autoAuraBuyThread = nil,
autoUnequipBoosts = false,
stopEverything = false,
walkOnWater = true,
selectedTreadmill = nil, autoTreadmill = false, autoTreadmillThread = nil,
leaderboardData = {}
}
local rockCache = {}
local prevRepSpeed = 5
local nameMap = {}
local displayNameMap = {}
local toggleHand = false
local lastStatValues = {}
local statGains = {}
local statGainTimes = {}
local ui = {
toggleButtons = {},
dropdownSetters = {},
dropdownHeaderSetters = {},
}
-- ==================== KILLER STATE ====================
local killerState = {
selectedPlayers = {}, manualWhitelist = {}, autoWhitelist = {},
whitelistFriendsEnabled = false, killSelectedEnabled = false, killEveryoneEnabled = false,
autoGoodKarmaEnabled = false, autoEvilKarmaEnabled = false,
}
local killSources = { selected = {}, everyone = {}, goodKarma = {}, evilKarma = {} }
local disabledAttachments = {}
local anchoredParts = {}
local originalTorsoSize = {}
local killerToggleHand = false
-- ==================== ROCK DATA ====================
local rockData = {
["Tiny Rock"]        = {req = 0,       name = "Tiny Rock",           idx = 1, glitchValue = 0.025},
["Punching Rock"]    = {req = 10,      name = "Punching Rock",       idx = 1, glitchValue = 0.05},
["Large Rock"]       = {req = 100,     name = "Large Rock",          idx = 1, glitchValue = 0.075},
["Golden Rock"]      = {req = 5000,    name = "Golden Rock",         idx = 1, glitchValue = 0.2},
["Frozen Rock"]      = {req = 150000,  name = "Frozen Rock",         idx = 1, glitchValue = 0.375},
["Mystic Rock"]      = {req = 400000,  name = "Mystic Rock",         idx = 1, glitchValue = 0.75},
["Inferno Rock"]     = {req = 750000,  name = "Inferno Rock",        idx = 1, glitchValue = 1.125},
["Legend Rock"]      = {req = 1000000, name = "Rock Of Legends",     idx = 1, glitchValue = 2.5},
["Muscle King Rock"] = {req = 5000000, name = "Muscle King Mountain", idx = 1, glitchValue = 12.5},
["Jungle Rock"]      = {req = 10000000,name = "Ancient Jungle Rock", idx = 1, glitchValue = 16.25}
}
-- Gym data
local gymData = {
["Tiny Island"] = { ["Bench Press"] = {pos = Vector3.new(67.105286, 6.714245, 1984.269043), cost = 10}, ["Medium Bench Press"] = {pos = Vector3.new(-144.733521, 7.551205, 1977.866211), cost = 150} },
["Beach"] = { ["Deadlift"] = {pos = Vector3.new(137.632263, 3.314179, 94.581825), cost = 1500}, ["Medium Deadlift"] = {pos = Vector3.new(134.463394, 3.314179, 58.235218), cost = 2500}, ["Big Deadlift"] = {pos = Vector3.new(141.028931, 3.314180, 24.779715), cost = 4000}, ["Squat Rack"] = {pos = Vector3.new(239.955200, 3.314179, 97.715668), cost = 1000}, ["Medium Squat Rack"] = {pos = Vector3.new(236.428650, 3.314180, 62.278412), cost = 1500}, ["Big Bench Press"] = {pos = Vector3.new(-193.705612, 8.826573, 300.670166), cost = 400} },
["Legend Beach"] = { ["Boulder Throw"] = {pos = Vector3.new(-85.060844, 7.303731, -297.216919), cost = 3000}, ["Medium Boulder Throw"] = {pos = Vector3.new(-73.385582, 18.023537, -366.033295), cost = 7500}, ["Huge Deadlift"] = {pos = Vector3.new(-69.451859, 38.819893, -491.074005), cost = 8000}, ["Big Boulder Throw"] = {pos = Vector3.new(56.436710, 28.062868, -281.883698), cost = 15000}, ["Huge Boulder Throw"] = {pos = Vector3.new(38.193623, 43.351215, -408.763794), cost = 25000}, ["Big Bench Press"] = {pos = Vector3.new(194.909180, 12.640547, -314.712921), cost = 3000}, ["Huge Bench Press"] = {pos = Vector3.new(198.874435, 20.215279, -374.683929), cost = 4000}, ["Very Big Bench Press"] = {pos = Vector3.new(475.157410, 24.176254, -259.730591), cost = 10000}, ["Very Huge Bench Press"] = {pos = Vector3.new(463.311310, 29.884830, -327.976959), cost = 15000} },
["Frozen Island"] = { ["Frozen Press"] = {pos = Vector3.new(-2604.636475, 10.087743, -177.873322), cost = 1000}, ["Medium Frozen Press"] = {pos = Vector3.new(-2750.141846, 16.310598, -185.355408), cost = 3000}, ["Big Frozen Press"] = {pos = Vector3.new(-3022.960938, 41.478535, -201.070541), cost = 7500}, ["Huge Frozen Press"] = {pos = Vector3.new(-3004.811035, 49.862091, -341.377167), cost = 15000}, ["Deadlift"] = {pos = Vector3.new(-2917.706543, 42.641796, -210.984131), cost = 5000}, ["Frozen Squat"] = {pos = Vector3.new(-2626.900146, 20.156042, -606.871155), cost = 4000}, ["Medium Frozen Squat"] = {pos = Vector3.new(-2721.806885, 30.932962, -590.231323), cost = 10000} },
["Mystic Island"] = { ["Mystic Pullup"] = {pos = Vector3.new(2259.983643, 21.162369, 838.979309), cost = 4000}, ["Medium Mystic Pullup"] = {pos = Vector3.new(2486.551025, 35.004158, 849.089417), cost = 8000}, ["Mystic Press"] = {pos = Vector3.new(2368.526367, 57.047935, 1242.839111), cost = 15000}, ["Mystic Throw"] = {pos = Vector3.new(2490.968506, 34.769260, 1240.426147), cost = 10000}, ["Medium Mystic Throw"] = {pos = Vector3.new(2575.198730, 48.729973, 1220.895142), cost = 18000}, ["Huge Mystic Throw"] = {pos = Vector3.new(2664.394287, 57.635010, 1204.904785), cost = 25000} },
["Inferno Island"] = { ["Eternal Press"] = {pos = Vector3.new(-7171.678223, 70.458687, -1100.657837), cost = 15000} },
["Legend Island"] = { ["Legend Lift"] = {pos = Vector3.new(4528.977539, 1027.182007, -4007.151367)}, ["Legend Squat"] = {pos = Vector3.new(4438.002930, 1021.915283, -4058.800537)}, ["Legend Pullup"] = {pos = Vector3.new(4306.121094, 1024.362549, -4121.852539)}, ["Legend Press"] = {pos = Vector3.new(4111.515137, 1041.059570, -3799.606445)}, ["Legend Throw"] = {pos = Vector3.new(4256.106934, 1008.502441, -3624.105713)}, ["Medium Legend Throw"] = {pos = Vector3.new(4184.614746, 1025.216309, -3904.958008)} },
["Muscle King Island"] = { ["Muscle King Lift"] = {pos = Vector3.new(-8773.583984, 48.473736, -5661.329590)}, ["Muscle King Throw"] = {pos = Vector3.new(-8945.077148, 63.497959, -5693.013672)}, ["Muscle King Squat"] = {pos = Vector3.new(-8756.472656, 43.382732, -6044.728027)}, ["Muscle King Press"] = {pos = Vector3.new(-8591.196289, 62.720314, -6043.746094)} },
["Jungle Island"] = { ["Jungle Bench"] = {pos = Vector3.new(-8627.859375, 50.873047, 1893.214722), cost = 25000}, ["Medium Jungle Bench"] = {pos = Vector3.new(-8430.405273, 76.713478, 1903.381836), cost = 50000}, ["Big Jungle Bench"] = {pos = Vector3.new(-8174.530273, 92.738068, 1916.532837), cost = 100000}, ["Jungle Bar Lift"] = {pos = Vector3.new(-8654.461914, 42.706390, 2086.785400), cost = 100000}, ["Jungle Boulder"] = {pos = Vector3.new(-8617.403320, 51.352287, 2677.590576), cost = 75000}, ["Jungle Squat"] = {pos = Vector3.new(-8581.711914, 36.457954, 2885.841797), cost = 50000}, ["Medium Jungle Squat"] = {pos = Vector3.new(-8375.843750, 56.552425, 2863.896729), cost = 125000} }
}
local treadmillData = {
["Tiny Treadmill"] = { exp = 1, pos = Vector3.new(237.729019, 9.687672, 387.652649) }, ["Small Treadmill"] = { exp = 2, pos = Vector3.new(238.908203, 9.687671, 358.320312) }, ["Medium Treadmill"] = { exp = 3, pos = Vector3.new(232.890976, 10.313229, 326.141510) }, ["Large Treadmill"] = { exp = 4, pos = Vector3.new(224.219528, 11.157012, 279.926788) }, ["Big Treadmill"] = { exp = 5, pos = Vector3.new(212.168045, 12.437231, 220.112839) }, ["Mystic Treadmill"] = { exp = 6, pos = Vector3.new(2658.494873, 25.894060, 932.837830) }, ["Legend Treadmill"] = { exp = 7, pos = Vector3.new(4368.882324, 1003.612976, -3639.254395) }, ["Jungle Treadmill"] = { exp = 8, pos = Vector3.new(-8132.056152, 32.226238, 2815.331787) }
}
-- ==================== UTILITIES ====================
local function getRockInstance(configName)
local data = rockData[configName]; if not data then return nil end
local folder = workspace:FindFirstChild("machinesFolder"); if not folder then return nil end
local found = {}; for _, v in ipairs(folder:GetChildren()) do if v.Name == data.name then table.insert(found, v) end end
return found[data.idx]
end
local function getPunch()
local bp = player:FindFirstChild("Backpack"); local p = bp and bp:FindFirstChild("Punch")
if not p and player.Character then p = player.Character:FindFirstChild("Punch") end; return p
end
local function formatNum(n)
local num = tonumber(n); if not num then return tostring(n) end
if states.withoutFormatNum then return tostring(num) end
local function trunc(val) return math.floor(val * 100) / 100 end
if num >= 1e18 then return string.format("%.2fQi", trunc(num / 1e18))
elseif num >= 1e15 then return string.format("%.2fQa", trunc(num / 1e15))
elseif num >= 1e12 then return string.format("%.2fT", trunc(num / 1e12))
elseif num >= 1e9 then return string.format("%.2fB", trunc(num / 1e9))
elseif num >= 1e6 then return string.format("%.2fM", trunc(num / 1e6))
elseif num >= 1e3 then return string.format("%.2fK", trunc(num / 1e3))
else return tostring(num) end
end
local function makeDraggable(gui)
local dragging, dragInput, dragStart, startPos
gui.InputBegan:Connect(function(input)
if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
dragging = true; dragStart = input.Position; startPos = gui.Position
input.Changed:Connect(function() if input.UserInputState == Enum.UserInputState.End then dragging = false end end)
end end)
gui.InputChanged:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then dragInput = input end end)
UserInputService.InputChanged:Connect(function(input)
if input == dragInput and dragging then
local delta = input.Position - dragStart
gui.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
end end)
end
local function ServerHop(targetType)
local sortOrder = targetType == "Small" and "Asc" or "Desc"
local url = "https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=" .. sortOrder .. "&limit=100"
local success, result = pcall(function() return HttpService:JSONDecode(game:HttpGet(url)) end)
if success and result and result.data then
for _, server in ipairs(result.data) do
if type(server) == "table" and server.playing and server.maxPlayers and server.id ~= game.JobId then
if server.playing > 0 and server.playing < server.maxPlayers then
if targetType == "Small" and server.playing <= 2 then TeleportService:TeleportToPlaceInstance(game.PlaceId, server.id, player); return
elseif targetType == "Large" then TeleportService:TeleportToPlaceInstance(game.PlaceId, server.id, player); return end
end end end end end
end
local function handleGlitchCleanup(method, callback)
local p = getPunch(); if p and p:FindFirstChild("attackTime") then p.attackTime.Value = 0.35 end
if method == "Teleport" then
if states.preGlitchPos and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then player.Character.HumanoidRootPart.CFrame = states.preGlitchPos end
if callback then callback() end
elseif method == "NonTeleport" then
states.isTransitioning = true; task.spawn(function() task.wait(1)
for rock, data in pairs(rockCache) do if rock and rock.Parent then rock.Size = data.Size; rock.CanCollide = data.CanCollide; rock.CFrame = data.CFrame; rock.Anchored = data.Anchored end end
table.clear(rockCache); states.isTransitioning = false; if callback then callback() end end)
else if callback then callback() end end
end
local function isLocalPlayerMuscleKing()
local mkPart = workspace:FindFirstChild("currentMuscleKingPart"); if not mkPart then return false end
local mkGui = mkPart:FindFirstChild("currentMuscleKingGui"); if not mkGui then return false end
local mkLabel = mkGui:FindFirstChild("currentMuscleKingLabel"); if not mkLabel or not mkLabel:IsA("TextLabel") then return false end
return mkLabel.Text == "Muscle King: " .. player.DisplayName
end
local function isMuscleKingNone()
local mkPart = workspace:FindFirstChild("currentMuscleKingPart"); if not mkPart then return true end
local mkGui = mkPart:FindFirstChild("currentMuscleKingGui"); if not mkGui then return true end
local mkLabel = mkGui:FindFirstChild("currentMuscleKingLabel"); if not mkLabel or not mkLabel:IsA("TextLabel") then return true end
return mkLabel.Text == "Muscle King: None"
end
local function getStrength() return player.leaderstats and player.leaderstats:FindFirstChild("Strength") and player.leaderstats.Strength.Value or 0 end
-- ==================== AUTO GYM & TREADMILL ====================
local function findBestSeat(nearPos)
local folder = workspace:FindFirstChild("machinesFolder"); if not folder then return nil end
local bestPart, bestDist = nil, math.huge
for _, obj in ipairs(folder:GetDescendants()) do if obj:IsA("Seat") or obj:IsA("VehicleSeat") then local dist = (obj.Position - nearPos).Magnitude; if dist < bestDist and dist < 50 then bestDist = dist; bestPart = obj end end end
return bestPart
end
local function fireMachineInteract(seat) pcall(function() ReplicatedStorage.rEvents.machineInteractRemote:InvokeServer("useMachine", seat) end) end
local function stopAutoGym() if states.autoGymThread then pcall(task.cancel, states.autoGymThread); states.autoGymThread = nil end; states.autoGym = false end
local function startAutoGym()
stopAutoGym(); if not states.selectedMap or not states.selectedGym then return end; states.autoGym = true; local lastTpTime = 0
states.autoGymThread = task.spawn(function()
while states.autoGym and states.selectedMap and states.selectedGym do task.wait(); if states.stopEverything then continue end
local mapData = gymData[states.selectedMap]; if not mapData then continue end; local gym = mapData[states.selectedGym]; if not gym then continue end
if gym.cost and getStrength() < gym.cost then continue end
if states.selectedMap == "Muscle King Island" and not isLocalPlayerMuscleKing() and not isMuscleKingNone() then continue end
local char = player.Character; if not char then continue end; local root = char:FindFirstChild("HumanoidRootPart"); local hum = char:FindFirstChild("Humanoid")
if not root or not hum or hum.Health <= 0 or hum.Sit then continue end
if tick() - lastTpTime >= 0.5 then pcall(function() root.CFrame = CFrame.new(gym.pos + Vector3.new(0, 4, 0)); root.AssemblyLinearVelocity = Vector3.zero end); lastTpTime = tick() end
local seat = findBestSeat(gym.pos); if seat then fireMachineInteract(seat) end end; states.autoGym = false end)
end
local function stopAutoTreadmill() if states.autoTreadmillThread then pcall(task.cancel, states.autoTreadmillThread); states.autoTreadmillThread = nil end; states.autoTreadmill = false end
local function startAutoTreadmill()
stopAutoTreadmill(); if not states.selectedTreadmill then return end; states.autoTreadmill = true
states.autoTreadmillThread = task.spawn(function()
while states.autoTreadmill and states.selectedTreadmill do task.wait(); if states.stopEverything then continue end
local treadmill = treadmillData[states.selectedTreadmill]; if not treadmill then continue end
local char = player.Character; if not char then continue end; local root = char:FindFirstChild("HumanoidRootPart"); local hum = char:FindFirstChild("Humanoid")
if not root or not hum or hum.Health <= 0 or hum.Sit then continue end
pcall(function() root.CFrame = CFrame.new(treadmill.pos + Vector3.new(0, 4, 0)); root.AssemblyLinearVelocity = Vector3.zero end); hum:Move(Vector3.new(0, 0, -1), true) end; states.autoTreadmill = false end)
end
-- ==================== AUTO CRYSTAL ====================
local function fireBuyCrystal(data) if not data then return end; pcall(function() ReplicatedStorage.cPetShopRemote:InvokeServer(data.folderChild) end) end
local function stopAutoPetBuy() if states.autoPetBuyThread then pcall(task.cancel, states.autoPetBuyThread); states.autoPetBuyThread = nil end; states.autoPetBuy = false end
local function startAutoPetBuy() if not states.selectedPetCrystalData then return end; stopAutoPetBuy(); states.autoPetBuy = true; states.autoPetBuyThread = task.spawn(function() while states.autoPetBuy do if states.stopEverything then task.wait(); continue end; fireBuyCrystal(states.selectedPetCrystalData); task.wait(0.8) end end) end
local function stopAutoAuraBuy() if states.autoAuraBuyThread then pcall(task.cancel, states.autoAuraBuyThread); states.autoAuraBuyThread = nil end; states.autoAuraBuy = false end
local function startAutoAuraBuy() if not states.selectedAuraCrystalData then return end; stopAutoAuraBuy(); states.autoAuraBuy = true; states.autoAuraBuyThread = task.spawn(function() while states.autoAuraBuy do if states.stopEverything then task.wait(); continue end; fireBuyCrystal(states.selectedAuraCrystalData); task.wait(0.8) end end) end
-- ==================== LEADERBOARD ====================
local function checkAllPlayersStrength()
local leaderboardData = {}; local players = Players:GetPlayers()
for _, plr in ipairs(players) do
local strength, rebirth, durability = 0, 0, 0
if plr.leaderstats then local strStat = plr.leaderstats:FindFirstChild("Strength"); if strStat then strength = strStat.Value end; local rebStat = plr.leaderstats:FindFirstChild("Rebirths"); if rebStat then rebirth = rebStat.Value end end
local durStat = plr:FindFirstChild("Durability"); if durStat then durability = durStat.Value end
table.insert(leaderboardData, { name = plr.DisplayName .. " (@" .. plr.Name .. ")", strength = strength, rebirth = rebirth, durability = durability })
end; table.sort(leaderboardData, function(a, b) return a.strength > b.strength end); states.leaderboardData = leaderboardData; return leaderboardData
end
-- ==================== KILLER UTILITIES ====================
local function getPlayerDisplayString(p) return p.DisplayName == p.Name and p.Name or p.DisplayName .. " (" .. p.Name .. ")" end
local function getUsernameFromDisplay(display) local startParen = display:find(" %("); if startParen then return display:sub(startParen + 2, -2) else return display end end
local function isWhitelisted(username) return killerState.manualWhitelist[username] or killerState.autoWhitelist[username] end
local function findTorso(character) return character:FindFirstChild("Torso") or character:FindFirstChild("UpperTorso") or character:FindFirstChild("HumanoidRootPart") end
-- ==================== UI FRAMEWORK ====================
local ScreenGui = Instance.new("ScreenGui", player.PlayerGui); ScreenGui.Name = "MuscleLegendsHub"; ScreenGui.ResetOnSpawn = false; getgenv().ML_Expert_UI = ScreenGui
local MainFrame = Instance.new("Frame", ScreenGui); MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25); MainFrame.BorderSizePixel = 0; MainFrame.Position = UDim2.fromScale(0.3, 0.3); MainFrame.Active = true; makeDraggable(MainFrame)
local TitleBar = Instance.new("Frame", MainFrame); TitleBar.Size = UDim2.new(1, 0, 0, 30); TitleBar.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
local TitleText = Instance.new("TextLabel", TitleBar); TitleText.Size = UDim2.new(1, -10, 1, 0); TitleText.Position = UDim2.fromOffset(5, 0); TitleText.BackgroundTransparency = 1; TitleText.Text = "Muscle Legends"; TitleText.TextColor3 = Color3.new(1, 1, 1); TitleText.TextXAlignment = Enum.TextXAlignment.Left
if UserInputService.TouchEnabled then
MainFrame.Size = UDim2.fromOffset(320, 380); MainFrame.Visible = false
local MobileToggle = Instance.new("TextButton", ScreenGui); MobileToggle.Size = UDim2.fromOffset(45, 45); MobileToggle.Position = UDim2.fromScale(0.1, 0.1); MobileToggle.Text = "ML"; MobileToggle.BackgroundColor3 = Color3.fromRGB(35, 35, 35); MobileToggle.TextColor3 = Color3.new(1, 1, 1); makeDraggable(MobileToggle)
MobileToggle.MouseButton1Click:Connect(function() MainFrame.Visible = not MainFrame.Visible end)
else
MainFrame.Size = UDim2.fromOffset(650, 500)
UserInputService.InputBegan:Connect(function(input, gpe) if not gpe and input.KeyCode == Enum.KeyCode.Z then MainFrame.Visible = not MainFrame.Visible end end)
end
local MainContent = Instance.new("Frame", MainFrame); MainContent.Size = UDim2.new(1, 0, 1, -30); MainContent.Position = UDim2.fromOffset(0, 30); MainContent.BackgroundTransparency = 1
local LeftPanel = Instance.new("Frame", MainContent); local RightPanel = Instance.new("Frame", MainContent)
if isMobile then LeftPanel.Size = UDim2.new(1, 0, 0, 35); LeftPanel.Position = UDim2.fromOffset(0, 0); LeftPanel.BackgroundColor3 = Color3.fromRGB(30, 30, 30); RightPanel.Size = UDim2.new(1, 0, 1, -35); RightPanel.Position = UDim2.fromOffset(0, 35); RightPanel.BackgroundTransparency = 1
else local leftWidth = 120; LeftPanel.Size = UDim2.new(0, leftWidth, 1, 0); LeftPanel.Position = UDim2.fromOffset(0, 0); LeftPanel.BackgroundColor3 = Color3.fromRGB(30, 30, 30); RightPanel.Size = UDim2.new(1, -leftWidth, 1, 0); RightPanel.Position = UDim2.fromOffset(leftWidth, 0); RightPanel.BackgroundTransparency = 1 end
local SidebarScroll = Instance.new("ScrollingFrame", LeftPanel); SidebarScroll.Size = UDim2.new(1, 0, 1, 0); SidebarScroll.BackgroundTransparency = 1; SidebarScroll.ScrollingEnabled = true; SidebarScroll.ClipsDescendants = true
local sideLayout = Instance.new("UIListLayout", SidebarScroll)
if isMobile then SidebarScroll.ScrollBarThickness = 2; SidebarScroll.ScrollingDirection = Enum.ScrollingDirection.X; sideLayout.FillDirection = Enum.FillDirection.Horizontal; sideLayout.HorizontalAlignment = Enum.HorizontalAlignment.Left; sideLayout.VerticalAlignment = Enum.VerticalAlignment.Center; sideLayout.Padding = UDim.new(0, 4)
else SidebarScroll.ScrollBarThickness = 0; SidebarScroll.ScrollingDirection = Enum.ScrollingDirection.Y; sideLayout.FillDirection = Enum.FillDirection.Vertical; sideLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center; sideLayout.Padding = UDim.new(0, 2) end
local frames = {}; local tabs = {"Auto Farm", "Farm", "Stats", "Auto Gym", "Auto Treadmill", "Auto Crystal", "Glitchable Rebirth", "Calculator", "Teleport", "Leaderboard", "Config", "Killer"}
if isMobile then SidebarScroll.CanvasSize = UDim2.new(0, #tabs * 75, 0, 0); SidebarScroll.AutomaticCanvasSize = Enum.AutomaticSize.None else SidebarScroll.CanvasSize = UDim2.new(0, 0, 0, #tabs * 32) end
for i, tName in ipairs(tabs) do
local btn = Instance.new("TextButton", SidebarScroll)
if isMobile then btn.Size = UDim2.new(0, 70, 1, -4); btn.TextSize = 9 else btn.Size = UDim2.new(1, -4, 0, 30); btn.TextSize = 10 end
btn.BackgroundColor3 = Color3.fromRGB(35, 35, 35); btn.Text = tName; btn.TextColor3 = Color3.new(1, 1, 1)
local f = Instance.new("ScrollingFrame", RightPanel); f.Size = UDim2.new(1, 0, 1, 0); f.BackgroundTransparency = 1; f.Visible = (i == 1); f.CanvasSize = UDim2.new(0, 0, 0, 0); f.AutomaticCanvasSize = Enum.AutomaticSize.Y; f.ScrollingEnabled = true; f.ClipsDescendants = true; f.ElasticBehavior = Enum.ElasticBehavior.WhenScrollable; f.ScrollBarThickness = isMobile and 6 or 2; f.ScrollingDirection = Enum.ScrollingDirection.Y
local layout = Instance.new("UIListLayout", f); layout.Padding = UDim.new(0, 5); frames[tName] = f
btn.MouseButton1Click:Connect(function() for _, other in pairs(frames) do other.Visible = false end; f.Visible = true end)
end
-- ==================== UI COMPONENTS HELPER ====================
local SetToggle = {}
local function createToggle(parent, text, key, callback)
local btn = Instance.new("TextButton", parent); btn.Size = UDim2.new(1, -10, 0, 30); btn.BackgroundColor3 = Color3.fromRGB(45, 45, 45); btn.Text = text .. " [OFF]"; btn.TextColor3 = Color3.new(1, 1, 1)
local state = false; if key then ui.toggleButtons[key] = btn end
SetToggle[btn] = function(newState) state = newState; btn.Text = text .. (state and " [ON]" or " [OFF]"); btn.BackgroundColor3 = state and Color3.fromRGB(0, 120, 0) or Color3.fromRGB(45, 45, 45) end
btn.MouseButton1Click:Connect(function() local newState = not state; if callback(newState) == false then newState = false end; SetToggle[btn](newState) end)
return btn
end
local function createConfirmPopup(message, onConfirm)
local popup = Instance.new("Frame", ScreenGui); popup.Size = UDim2.fromOffset(280, 120); popup.Position = UDim2.new(0.5, -140, 0.5, -60); popup.BackgroundColor3 = Color3.fromRGB(35, 35, 35); popup.BorderSizePixel = 0; popup.ZIndex = 100; Instance.new("UICorner", popup).CornerRadius = UDim.new(0, 8)
local label = Instance.new("TextLabel", popup); label.Size = UDim2.new(1, -20, 0, 50); label.Position = UDim2.fromOffset(10, 10); label.Text = message; label.TextColor3 = Color3.new(1, 1, 1); label.TextWrapped = true; label.BackgroundTransparency = 1; label.ZIndex = 101
local btnWidth = 120; local yesBtn = Instance.new("TextButton", popup); yesBtn.Size = UDim2.fromOffset(btnWidth, 35); yesBtn.Position = UDim2.fromOffset(10, 70); yesBtn.Text = "Yes"; yesBtn.BackgroundColor3 = Color3.fromRGB(0, 140, 0); yesBtn.TextColor3 = Color3.new(1, 1, 1); yesBtn.ZIndex = 101; yesBtn.MouseButton1Click:Connect(function() popup:Destroy(); onConfirm(true) end)
local noBtn = Instance.new("TextButton", popup); noBtn.Size = UDim2.fromOffset(btnWidth, 35); noBtn.Position = UDim2.fromOffset(280 - 10 - btnWidth, 70); noBtn.Text = "No"; noBtn.BackgroundColor3 = Color3.fromRGB(140, 0, 0); noBtn.TextColor3 = Color3.new(1, 1, 1); noBtn.ZIndex = 101; noBtn.MouseButton1Click:Connect(function() popup:Destroy(); onConfirm(false) end)
end
local function createDropdown(parent, text, options, multi, callback, checkStateFunc)
local container = Instance.new("Frame", parent); container.Size = UDim2.new(1, -10, 0, 30); container.BackgroundTransparency = 1
local headerText = Instance.new("TextLabel", container); headerText.Size = UDim2.new(1, 0, 0, 30); headerText.BackgroundColor3 = Color3.fromRGB(50, 50, 50); headerText.Text = text .. " v"; headerText.TextColor3 = Color3.new(1, 1, 1); headerText.Active = true
local headerBtn = Instance.new("TextButton", container); headerBtn.Size = UDim2.new(1, 0, 0, 30); headerBtn.BackgroundTransparency = 1; headerBtn.Text = ""; headerBtn.ZIndex = 12
local list = Instance.new("ScrollingFrame", container); list.Position = UDim2.fromOffset(0, 30); list.Visible = false; list.ZIndex = 10; list.BackgroundColor3 = Color3.fromRGB(40, 40, 40); list.ScrollBarThickness = isMobile and 6 or 4; list.ScrollingEnabled = true; list.ClipsDescendants = true; list.ElasticBehavior = Enum.ElasticBehavior.WhenScrollable
headerBtn.MouseButton1Click:Connect(function() list.Visible = not list.Visible; local listHeight = math.min(#options * 25, 150); container.Size = list.Visible and UDim2.new(1, -10, 0, listHeight + 30) or UDim2.new(1, -10, 0, 30); list.Size = UDim2.new(1, 0, 0, listHeight); list.CanvasSize = UDim2.new(0, 0, 0, #options * 25) end)
local function refresh(newOpts)
if newOpts then options = newOpts end; for _, v in ipairs(list:GetChildren()) do v:Destroy() end
for i, opt in ipairs(options) do
local o = Instance.new("TextButton", list); o.Size = UDim2.new(1, 0, 0, 25); o.Position = UDim2.fromOffset(0, (i-1)*25); o.Text = tostring(opt); o.TextColor3 = Color3.new(1, 1, 1); o.ZIndex = 11
if multi and checkStateFunc then local isActive = checkStateFunc(opt); o.BackgroundColor3 = isActive and Color3.fromRGB(0, 150, 0) or Color3.fromRGB(60, 60, 60) else o.BackgroundColor3 = Color3.fromRGB(60, 60, 60) end
o.MouseButton1Click:Connect(function()
if not multi then headerText.Text = tostring(opt) .. " v"; list.Visible = false; container.Size = UDim2.new(1, -10, 0, 30); callback(opt)
else local newState = callback(opt); o.BackgroundColor3 = newState and Color3.fromRGB(0, 150, 0) or Color3.fromRGB(60, 60, 60) end
end) end
local listHeight = math.min(#options * 25, 150); if list.Visible then container.Size = UDim2.new(1, -10, 0, listHeight + 30); list.Size = UDim2.new(1, 0, 0, listHeight) end; list.CanvasSize = UDim2.new(0, 0, 0, #options * 25)
end; refresh()
return { refresh = refresh, setHeaderText = function(txt) headerText.Text = txt end, setValue = function(val) if not multi then headerText.Text = tostring(val) .. " v"; callback(val) end end, optionsList = options }
end
-- ==================== TAB 1: AUTO FARM ====================
local f1 = frames["Auto Farm"]
ui.selectRockDD = createDropdown(f1, "Select Rock", {"Tiny Rock", "Punching Rock", "Large Rock", "Golden Rock", "Frozen Rock", "Mystic Rock", "Inferno Rock", "Legend Rock", "Muscle King Rock", "Jungle Rock"}, false, function(v) if states.autoGlitch then handleGlitchCleanup(states.selectedMethod, function() states.selectedRock = v end) else states.selectedRock = v end end)
ui.dropdownSetters["selectRock"] = ui.selectRockDD; ui.dropdownHeaderSetters["selectRock"] = ui.selectRockDD
ui.selectMethodDD = createDropdown(f1, "Select Method", {"Teleport", "NonTeleport"}, false, function(v) if states.autoGlitch and states.selectedMethod ~= v then handleGlitchCleanup(states.selectedMethod, function() states.selectedMethod = v; if v == "Teleport" and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then states.preGlitchPos = player.Character.HumanoidRootPart.CFrame end end) else states.selectedMethod = v end end)
ui.dropdownSetters["selectMethod"] = ui.selectMethodDD; ui.dropdownHeaderSetters["selectMethod"] = ui.selectMethodDD
ui.toolDD = createDropdown(f1, "Tool Dropdown", {"Weight", "Pushups", "Situps", "Handstands", "Punch"}, false, function(v) states.selectedTool = v end)
ui.dropdownSetters["toolDropdown"] = ui.toolDD; ui.dropdownHeaderSetters["toolDropdown"] = ui.toolDD
ui.autoGlitchTrainBtn = createToggle(f1, "Auto Glitch", "autoGlitch", function(val)
if val then if not states.selectedRock or not states.selectedMethod then return false end; local req = rockData[states.selectedRock].req; local myDur = player:FindFirstChild("Durability") and player.Durability.Value or 0; if myDur < req then return false end; if states.selectedMethod == "Teleport" and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then states.preGlitchPos = player.Character.HumanoidRootPart.CFrame end; states.autoGlitch = true; states.glitchAccumulated = 0; states.glitchStartTime = os.time(); return true
else states.autoGlitch = false; states.glitchAccumulated = states.glitchAccumulated + (os.time() - states.glitchStartTime); if states.autoGlitchTrain then states.autoGlitchTrain = false; if ui.toggleButtons["autoGlitchTrain"] then SetToggle[ui.toggleButtons["autoGlitchTrain"]](false) end end; handleGlitchCleanup(states.selectedMethod); return false end end)
local timerLabel = Instance.new("TextLabel", f1); timerLabel.Size = UDim2.new(1, -10, 0, 20); timerLabel.BackgroundTransparency = 1; timerLabel.TextColor3 = Color3.fromRGB(200, 200, 200); timerLabel.Text = "Glitch Timer: 00:00:00:00"
local pingFpsLabel = Instance.new("TextLabel", f1); pingFpsLabel.Size = UDim2.new(1, -10, 0, 25); pingFpsLabel.BackgroundTransparency = 1; pingFpsLabel.TextColor3 = Color3.fromRGB(100, 255, 100); pingFpsLabel.Font = Enum.Font.GothamBold; pingFpsLabel.TextSize = 12; pingFpsLabel.Text = "Ping: -- ms | FPS: --"
table.insert(getgenv().ML_Expert_Loops, task.spawn(function() local lastTime = tick(); local frameCount = 0; while task.wait() do frameCount = frameCount + 1; local currentTime = tick(); if currentTime - lastTime >= 1 then local fps = frameCount; frameCount = 0; lastTime = currentTime; local ping = Stats:FindFirstChild("Network") and Stats.Network:FindFirstChild("ServerStatsItem") and Stats.Network.ServerStatsItem:FindFirstChild("Data Ping"); local pingValue = ping and ping:GetValueString() or "??"; pingFpsLabel.Text = string.format("Ping: %s ms | FPS: %d", pingValue, fps) end; local total = states.glitchAccumulated; if states.autoGlitch then total = total + (os.time() - states.glitchStartTime) end; local d, h, m, s = math.floor(total / 86400), math.floor((total % 86400) / 3600), math.floor((total % 3600) / 60), total % 60; timerLabel.Text = string.format("Glitch Timer: %02d:%02d:%02d:%02d", d, h, m, s) end end))
createToggle(f1, "Auto Glitch While Training", "autoGlitchTrain", function(val) if val and not states.autoGlitch then return false end; states.autoGlitchTrain = val; return val end)
ui.autoLiftNoAnimBtn = createToggle(f1, "Auto Lift (Without Anim)", "autoLiftNoAnim", function(val) states.autoLiftNoAnim = val; if val and ui.toggleButtons["autoLiftValue"] then states.autoLiftValue = false; if player:FindFirstChild("autoLiftEnabled") then player.autoLiftEnabled.Value = false end; SetToggle[ui.toggleButtons["autoLiftValue"]](false) end end)
ui.autoLiftBtn = createToggle(f1, "Auto Lift", "autoLiftValue", function(val) states.autoLiftValue = val; if player:FindFirstChild("autoLiftEnabled") then player.autoLiftEnabled.Value = val end; if val and ui.toggleButtons["autoLiftNoAnim"] then states.autoLiftNoAnim = false; SetToggle[ui.toggleButtons["autoLiftNoAnim"]](false) end end)
createToggle(f1, "Auto Equip Tool", "autoEquip", function(v) if v and (not states.selectedTool or states.selectedTool == "NO TOOL FOUND") then return false end; states.autoEquip = v; return v end)
local rebBox = Instance.new("TextBox", f1); rebBox.Size = UDim2.new(1, -10, 0, 30); rebBox.Text = Config.RebirthTarget; rebBox.PlaceholderText = "Rebirth Target"; rebBox.BackgroundColor3 = Color3.fromRGB(40, 40, 40); rebBox.TextColor3 = Color3.new(1, 1, 1); rebBox.FocusLost:Connect(function() Config.RebirthTarget = rebBox.Text; SaveConfig() end)
do
local function createRebirthConfirmToggle(parent, text, key, stateKey, otherStateKey, otherKey)
local btn = Instance.new("TextButton", parent); btn.Size = UDim2.new(1, -10, 0, 30); btn.BackgroundColor3 = Color3.fromRGB(45, 45, 45); btn.Text = text .. " [OFF]"; btn.TextColor3 = Color3.new(1, 1, 1); local state = false; if key then ui.toggleButtons[key] = btn end
local function updateVisual() btn.Text = text .. (state and " [ON]" or " [OFF]"); btn.BackgroundColor3 = state and Color3.fromRGB(0, 120, 0) or Color3.fromRGB(45, 45, 45) end
SetToggle[btn] = function(newState) state = newState; updateVisual() end
btn.MouseButton1Click:Connect(function()
if state then state = false; updateVisual(); states[stateKey] = false
else createConfirmPopup("Are you sure you want to activate " .. text .. "?", function(confirmed) if confirmed then state = true; updateVisual(); states[stateKey] = true; if states[otherStateKey] then states[otherStateKey] = false; if otherKey and ui.toggleButtons[otherKey] then SetToggle[ui.toggleButtons[otherKey]](false) end end end end) end end)
return btn end
ui.autoRebirthTargetBtn = createRebirthConfirmToggle(f1, "Auto Rebirth Target", "autoRebirthTarget", "autoRebirthTarget", "autoRebirthInf", "autoRebirthInf")
ui.autoRebirthInfBtn = createRebirthConfirmToggle(f1, "Auto Rebirth Inf", "autoRebirthInf", "autoRebirthInf", "autoRebirthTarget", "autoRebirthTarget")
end
createToggle(f1, "Auto Muscle King", "autoMuscleKing", function(val) states.autoMuscleKing = val; if not val then local char = player.Character; if char and char:FindFirstChild("HumanoidRootPart") then local hrp = char.HumanoidRootPart; hrp.AssemblyLinearVelocity = Vector3.zero; hrp.AssemblyAngularVelocity = Vector3.zero end end; if val then states.lockedPos = nil; if ui.lockPosBtn and SetToggle[ui.lockPosBtn] then SetToggle[ui.lockPosBtn](false) end end; return val end)
createToggle(f1, "Auto Claim Gift", "autoClaim", function(v) states.autoClaim = v end)
createToggle(f1, "Auto Punch", "autoPunch", function(v) states.autoPunch = v end)
createToggle(f1, "Auto Fortune Wheel", "autoFortuneWheel", function(v) states.autoFortuneWheel = v end)
createToggle(f1, "Accelerate Rep Speed", "accelReps", function(val) states.accelReps = val; local ult = player:FindFirstChild("ultimatesFolder") and player.ultimatesFolder:FindFirstChild("+5% Rep Speed"); if ult then if val then prevRepSpeed = ult.Value; ult.Value = 1000 else ult.Value = prevRepSpeed end end end)
createToggle(f1, "Fast Punch", "fastPunch", function(val) states.fastPunch = val; local p = getPunch(); if p and p:FindFirstChild("attackTime") then p.attackTime.Value = val and 0 or 0.35 end end)
-- ==================== TAB 2: FARM ====================
local f2 = frames["Farm"]
createToggle(f2, "Less Lag", "lessLag", function(val) states.lessLag = val end)
local sizeBox = Instance.new("TextBox", f2); sizeBox.Size = UDim2.new(1, -10, 0, 30); sizeBox.Text = Config.SizeValue; sizeBox.PlaceholderText = "Size Value"; sizeBox.BackgroundColor3 = Color3.fromRGB(40, 40, 40); sizeBox.TextColor3 = Color3.new(1, 1, 1); sizeBox.FocusLost:Connect(function() local num = tonumber(sizeBox.Text); if not num or num < 1 then sizeBox.Text = "1" elseif num > 100 then sizeBox.Text = "100" else sizeBox.Text = tostring(math.floor(num)) end; Config.SizeValue = sizeBox.Text; SaveConfig() end)
createToggle(f2, "Auto Size Toggle", "autoSize", function(v) states.autoSize = v end)
local wsBox = Instance.new("TextBox", f2); wsBox.Size = UDim2.new(1, -10, 0, 30); wsBox.Text = Config.WalkSpeed or "16"; wsBox.PlaceholderText = "WalkSpeed"; wsBox.BackgroundColor3 = Color3.fromRGB(40, 40, 40); wsBox.TextColor3 = Color3.new(1, 1, 1); wsBox:GetPropertyChangedSignal("Text"):Connect(function() wsBox.Text = wsBox.Text:gsub("%D", "") end); wsBox.FocusLost:Connect(function() local num = tonumber(wsBox.Text); if not num or num < 1 then wsBox.Text = "16" else wsBox.Text = tostring(math.floor(num)) end; Config.WalkSpeed = wsBox.Text; SaveConfig() end)
createToggle(f2, "Auto WalkSpeed", "autoWalkSpeed", function(v) states.autoWalkSpeed = v end)
local flySpeedBox = Instance.new("TextBox", f2); flySpeedBox.Size = UDim2.new(1, -10, 0, 30); flySpeedBox.Text = Config.FlySpeed or "50"; flySpeedBox.PlaceholderText = "Fly Speed"; flySpeedBox.BackgroundColor3 = Color3.fromRGB(40, 40, 40); flySpeedBox.TextColor3 = Color3.new(1, 1, 1); flySpeedBox:GetPropertyChangedSignal("Text"):Connect(function() flySpeedBox.Text = flySpeedBox.Text:gsub("%D", "") end); flySpeedBox.FocusLost:Connect(function() Config.FlySpeed = flySpeedBox.Text; SaveConfig() end)
createToggle(f2, "Fly", "fly", function(v) states.fly = v; if not v and states.flyBody then if states.flyBody.Velocity.Parent then states.flyBody.Velocity:Destroy() end; if states.flyBody.Gyro.Parent then states.flyBody.Gyro:Destroy() end; states.flyBody = nil end; if not v and player.Character and player.Character:FindFirstChild("Humanoid") then player.Character.Humanoid.PlatformStand = false end end)
createToggle(f2, "God Mode", "godMode", function(v) states.godMode = v end)
local copyPosBtn = Instance.new("TextButton", f2); copyPosBtn.Size = UDim2.new(1, -10, 0, 35); copyPosBtn.Text = "Copy My Position"; copyPosBtn.BackgroundColor3 = Color3.fromRGB(40, 60, 60); copyPosBtn.TextColor3 = Color3.new(1, 1, 1); copyPosBtn.MouseButton1Click:Connect(function() if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then local pos = player.Character.HumanoidRootPart.Position; pcall(function() setclipboard(string.format("%f, %f, %f", pos.X, pos.Y, pos.Z)) end) end end)
local snackBtn = Instance.new("TextButton", f2); snackBtn.Size = UDim2.new(1, -10, 0, 35); snackBtn.Text = "Eat All Snacks"; snackBtn.BackgroundColor3 = Color3.fromRGB(60, 40, 40); snackBtn.TextColor3 = Color3.new(1, 1, 1)
snackBtn.MouseButton1Click:Connect(function() createConfirmPopup("Are you sure you want to eat ALL snacks? This cannot be undone.", function(confirmed) if confirmed then local skip = {Weight=1, Punch=1, ["Ground Slam"]=1, Stomp=1, Handstands=1, Situps=1, Pushups=1, ["Protein Egg"]=1}; local char = player.Character; if not char then return end; local snacksToEat = {}; for _, t in ipairs(player.Backpack:GetChildren()) do if t:IsA("Tool") and not skip[t.Name] then table.insert(snacksToEat, t) end end; for _, t in ipairs(snacksToEat) do t.Parent = char; t:Activate() end; task.wait(); for _, t in ipairs(snacksToEat) do t.Parent = player.Backpack end end end) end)
ui.lockPosBtn = createToggle(f2, "Lock Position", "lockPosition", function(val) if states.autoMuscleKing and val then return false end; if val then if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then states.lockedPos = player.Character.HumanoidRootPart.CFrame else return false end else states.lockedPos = nil; local char = player.Character; if char and char:FindFirstChild("HumanoidRootPart") then local hrp = char.HumanoidRootPart; hrp.AssemblyLinearVelocity = Vector3.zero; hrp.AssemblyAngularVelocity = Vector3.zero end end; return val end)
local walkOnWaterPlatform = nil; ui.walkOnWaterBtn = createToggle(f2, "Walk on Water", "walkOnWater", function(val) states.walkOnWater = val; if not val and walkOnWaterPlatform then walkOnWaterPlatform:Destroy(); walkOnWaterPlatform = nil end; return val end)
createToggle(f2, "UnEquip Boosts & Drinks", "autoUnequipBoosts", function(val) states.autoUnequipBoosts = val; return val end)
local smallHopBtn = Instance.new("TextButton", f2); smallHopBtn.Size = UDim2.new(1, -10, 0, 35); smallHopBtn.Text = "Small Server Hop"; smallHopBtn.BackgroundColor3 = Color3.fromRGB(40, 60, 60); smallHopBtn.TextColor3 = Color3.new(1, 1, 1); smallHopBtn.MouseButton1Click:Connect(function() ServerHop("Small") end)
local largeHopBtn = Instance.new("TextButton", f2); largeHopBtn.Size = UDim2.new(1, -10, 0, 35); largeHopBtn.Text = "Large Server Hop"; largeHopBtn.BackgroundColor3 = Color3.fromRGB(60, 40, 40); largeHopBtn.TextColor3 = Color3.new(1, 1, 1); largeHopBtn.MouseButton1Click:Connect(function() ServerHop("Large") end)
local rejoinBtn = Instance.new("TextButton", f2); rejoinBtn.Size = UDim2.new(1, -10, 0, 35); rejoinBtn.Text = "Rejoin"; rejoinBtn.BackgroundColor3 = Color3.fromRGB(60, 40, 60); rejoinBtn.TextColor3 = Color3.new(1, 1, 1); rejoinBtn.MouseButton1Click:Connect(function() pcall(function() TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, player) end) end)
local openAllChestsBtn = Instance.new("TextButton", f2); openAllChestsBtn.Size = UDim2.new(1, -10, 0, 35); openAllChestsBtn.Text = "Open All Chest"; openAllChestsBtn.BackgroundColor3 = Color3.fromRGB(60, 120, 60); openAllChestsBtn.TextColor3 = Color3.new(1, 1, 1)
openAllChestsBtn.MouseButton1Click:Connect(function() local rs = game:GetService("ReplicatedStorage"); local chests = {"Group Rewards", "Golden Chest", "Enchanted Chest", "Mythical Chest", "Magma Chest", "Legends Chest", "Jungle Chest"}; for _, chestName in ipairs(chests) do pcall(function() rs.rEvents.checkChestRemote:InvokeServer(chestName) end); task.wait(0.3) end end)
-- ==================== TAB 3: STATS & INVENTORY ====================
local f3 = frames["Stats"]
local statsPlayerDD; local basicDD, advDD, rareDD, epicDD, uniqueDD; local auraBasicDD, auraAdvDD, auraRareDD, auraEpicDD, auraUniqueDD
local petDetails, auraDetails; local petRefsBasic, petRefsAdv, petRefsRare, petRefsEpic, petRefsUnique = {}, {}, {}, {}, {}; local auraRefsBasic, auraRefsAdv, auraRefsRare, auraRefsEpic, auraRefsUnique = {}, {}, {}, {}, {}
-- ==================== PET/AURA TRACKING (FIXED) ====================
local itemStates = {}; local newItems = {}; local changedItems = {}
local activeConnections = {}; local activeWatchers = {}

local function getPetAuraState(item)
local state = {}; local evObj = item:FindFirstChild("Evolved") or item:FindFirstChild("evolved"); state.evolved = evObj and evObj:IsA("BoolValue") and evObj.Value or false
local expObj = item:FindFirstChild("exp"); state.exp = expObj and expObj.Value or 0
local lvlObj = item:FindFirstChild("level"); state.level = lvlObj and lvlObj.Value or 1
local reqObj = item:FindFirstChild("requiredRebirths"); state.reqReb = reqObj and reqObj.Value or 0
local perks = item:FindFirstChild("perksFolder"); if perks then for _, pName in ipairs({"strength", "durability", "agility", "damage"}) do local pObj = perks:FindFirstChild(pName); if pObj then state[pName] = pObj:IsA("ValueBase") and pObj.Value or (pObj:FindFirstChild("Value") and pObj.Value.Value) or 0 end end end
return state
end
local function compareStates(s1, s2) for k, v in pairs(s1) do if s2[k] ~= v then return false end end; for k, v in pairs(s2) do if s1[k] ~= v then return false end end; return true end

local function setupItemTracking(item, onStateChanged)
local connections = {}
local function checkChange()
if not itemStates[item] then return end
local newState = getPetAuraState(item)
if not compareStates(itemStates[item], newState) then
changedItems[item] = true; itemStates[item] = newState
if onStateChanged then onStateChanged() end
end end
local function connectVal(obj) if obj and obj:IsA("ValueBase") then table.insert(connections, obj:GetPropertyChangedSignal("Value"):Connect(checkChange)) end end
connectVal(item:FindFirstChild("Evolved") or item:FindFirstChild("evolved")); connectVal(item:FindFirstChild("exp")); connectVal(item:FindFirstChild("level")); connectVal(item:FindFirstChild("requiredRebirths"))
local perks = item:FindFirstChild("perksFolder"); if perks then for _, pName in ipairs({"strength", "durability", "agility", "damage"}) do local pObj = perks:FindFirstChild(pName); if pObj then if pObj:IsA("ValueBase") then connectVal(pObj) else connectVal(pObj:FindFirstChild("Value")) end end end end
activeConnections[item] = connections
end

local function refreshPetDropdown(folder, dropdownObj, refStore)
if not folder then dropdownObj.refresh({"Nothing Here"}); if refStore then table.clear(refStore) end; return end
local counts = {}; local options = {}; local itemRefs = {}
for _, item in ipairs(folder:GetChildren()) do
local baseName = item.Name; counts[baseName] = (counts[baseName] or 0) + 1
local cleanName = baseName .. " #" .. counts[baseName]; local finalName = cleanName
local isNew = newItems[item] == true; local isChanged = changedItems[item] == true
if isNew and isChanged then finalName = finalName .. " *NEW* *CHANGED*"
elseif isNew then finalName = finalName .. " *NEW*"
elseif isChanged then finalName = finalName .. " *CHANGED*" end
table.insert(options, finalName); itemRefs[cleanName] = item
end
if #options == 0 then options = {"Nothing Here"} end; dropdownObj.refresh(options)
if refStore then table.clear(refStore); for k, v in pairs(itemRefs) do refStore[k] = v end end
end

local function loadInitialPets(folder, dd, refs)
if not folder then return end
for _, item in ipairs(folder:GetChildren()) do
if not itemStates[item] then
itemStates[item] = getPetAuraState(item)
setupItemTracking(item, function() refreshPetDropdown(folder, dd, refs) end)
end end
refreshPetDropdown(folder, dd, refs)
end

local function setupFolderWatcher(folder, dropdownObj, refStore)
if not folder or activeWatchers[folder] then return end
local c1 = folder.ChildAdded:Connect(function(newItem)
newItems[newItem] = true; itemStates[newItem] = getPetAuraState(newItem)
setupItemTracking(newItem, function() refreshPetDropdown(folder, dropdownObj, refStore) end)
refreshPetDropdown(folder, dropdownObj, refStore)
end)
local c2 = folder.ChildRemoved:Connect(function(removedItem)
itemStates[removedItem] = nil; newItems[removedItem] = nil; changedItems[removedItem] = nil
refreshPetDropdown(folder, dropdownObj, refStore)
end)
activeWatchers[folder] = {c1, c2}
end

local function handleItemSelection(label, refTable, detailLabel, folder, dropdownObj)
if not label or label == "Nothing Here" then return end
local cleanLabel = label:gsub(" %*NEW%*", ""):gsub(" %*CHANGED%*", "")
local item = refTable[cleanLabel]
if item then
if changedItems[item] then
changedItems[item] = nil
if folder and dropdownObj then refreshPetDropdown(folder, dropdownObj, refTable) end
end
local newState = getPetAuraState(item); itemStates[item] = newState
local isEvolved = newState.evolved
local txt = "Evolved: " .. tostring(isEvolved) .. "\nXP: " .. formatNum(newState.exp) .. "\nLevel: " .. formatNum(newState.level) .. "\nRequires Rebirth: " .. formatNum(newState.reqReb) .. "\nPerks:\n"
for _, pName in ipairs({"strength", "durability", "agility", "damage"}) do local fv = newState[pName] or "Not Found"; txt = txt .. pName:sub(1,1):upper() .. pName:sub(2) .. ": " .. formatNum(fv) .. "\n" end
detailLabel.Text = txt
else detailLabel.Text = "Select a " .. (detailLabel == petDetails and "Pet" or "Aura") .. " to view stats" end
end

local function clearPetAuraUI()
states.currentPetLabel = nil; states.currentPetTable = nil; states.currentAuraLabel = nil; states.currentAuraTable = nil
if petDetails then petDetails.Text = "Select a Pet to view stats" end; if auraDetails then auraDetails.Text = "Select an Aura to view stats" end
if basicDD then basicDD.refresh({"Nothing Here"}); basicDD.setHeaderText("Basic Pets v") end; if advDD then advDD.refresh({"Nothing Here"}); advDD.setHeaderText("Advance Pets v") end; if rareDD then rareDD.refresh({"Nothing Here"}); rareDD.setHeaderText("Rare Pets v") end; if epicDD then epicDD.refresh({"Nothing Here"}); epicDD.setHeaderText("Epic Pets v") end; if uniqueDD then uniqueDD.refresh({"Nothing Here"}); uniqueDD.setHeaderText("Unique Pets v") end
if auraBasicDD then auraBasicDD.refresh({"Nothing Here"}); auraBasicDD.setHeaderText("Basic Auras v") end; if auraAdvDD then auraAdvDD.refresh({"Nothing Here"}); auraAdvDD.setHeaderText("Advance Auras v") end; if auraRareDD then auraRareDD.refresh({"Nothing Here"}); auraRareDD.setHeaderText("Rare Auras v") end; if auraEpicDD then auraEpicDD.refresh({"Nothing Here"}); auraEpicDD.setHeaderText("Epic Auras v") end; if auraUniqueDD then auraUniqueDD.refresh({"Nothing Here"}); auraUniqueDD.setHeaderText("Unique Auras v") end
end

local function resetStatsUI()
states.trackStats = false; states.statsTarget = nil; lastStatValues = {}; statGains = {}; statGainTimes = {}
for item, conns in pairs(activeConnections) do for _, conn in ipairs(conns) do pcall(function() conn:Disconnect() end) end end; table.clear(activeConnections)
for folder, conns in pairs(activeWatchers) do for _, conn in ipairs(conns) do pcall(function() conn:Disconnect() end) end end; table.clear(activeWatchers)
itemStates = {}; newItems = {}; changedItems = {}
if statsPlayerDD then statsPlayerDD.setHeaderText("Player Dropdown v") end; if ui.trackToggle then SetToggle[ui.trackToggle](false) end; clearPetAuraUI()
end

statsPlayerDD = createDropdown(f3, "Player Dropdown", {}, false, function(label)
local newTarget = nameMap[label] or label
if states.statsTarget ~= newTarget then
resetStatsUI(); states.statsTarget = newTarget; statsPlayerDD.setHeaderText(label .. " v")
local tp = Players:FindFirstChild(newTarget)
if tp then
local petsF = tp:FindFirstChild("petsFolder"); local powerUpsF = tp:FindFirstChild("powerUpsFolder")
loadInitialPets(petsF and petsF:FindFirstChild("Basic"), basicDD, petRefsBasic); setupFolderWatcher(petsF and petsF:FindFirstChild("Basic"), basicDD, petRefsBasic)
loadInitialPets(petsF and petsF:FindFirstChild("Advanced"), advDD, petRefsAdv); setupFolderWatcher(petsF and petsF:FindFirstChild("Advanced"), advDD, petRefsAdv)
loadInitialPets(petsF and petsF:FindFirstChild("Rare"), rareDD, petRefsRare); setupFolderWatcher(petsF and petsF:FindFirstChild("Rare"), rareDD, petRefsRare)
loadInitialPets(petsF and petsF:FindFirstChild("Epic"), epicDD, petRefsEpic); setupFolderWatcher(petsF and petsF:FindFirstChild("Epic"), epicDD, petRefsEpic)
loadInitialPets(petsF and petsF:FindFirstChild("Unique"), uniqueDD, petRefsUnique); setupFolderWatcher(petsF and petsF:FindFirstChild("Unique"), uniqueDD, petRefsUnique)
loadInitialPets(powerUpsF and powerUpsF:FindFirstChild("Basic"), auraBasicDD, auraRefsBasic); setupFolderWatcher(powerUpsF and powerUpsF:FindFirstChild("Basic"), auraBasicDD, auraRefsBasic)
loadInitialPets(powerUpsF and powerUpsF:FindFirstChild("Advanced"), auraAdvDD, auraRefsAdv); setupFolderWatcher(powerUpsF and powerUpsF:FindFirstChild("Advanced"), auraAdvDD, auraRefsAdv)
loadInitialPets(powerUpsF and powerUpsF:FindFirstChild("Rare"), auraRareDD, auraRefsRare); setupFolderWatcher(powerUpsF and powerUpsF:FindFirstChild("Rare"), auraRareDD, auraRefsRare)
loadInitialPets(powerUpsF and powerUpsF:FindFirstChild("Epic"), auraEpicDD, auraRefsEpic); setupFolderWatcher(powerUpsF and powerUpsF:FindFirstChild("Epic"), auraEpicDD, auraRefsEpic)
loadInitialPets(powerUpsF and powerUpsF:FindFirstChild("Unique"), auraUniqueDD, auraRefsUnique); setupFolderWatcher(powerUpsF and powerUpsF:FindFirstChild("Unique"), auraUniqueDD, auraRefsUnique)
end end end)
Players.PlayerRemoving:Connect(function(plr) if states.statsTarget == plr.Name then resetStatsUI() end end)
ui.trackToggle = createToggle(f3, "Track Stats", "trackStats", function(v) states.trackStats = v; if v and not states.statsTarget then return false end; return v end)
createToggle(f3, "Without Format Number", "withoutFormatNum", function(v) states.withoutFormatNum = v end)
local tpToPlayerBtn = Instance.new("TextButton", f3); tpToPlayerBtn.Size = UDim2.new(1, -10, 0, 30); tpToPlayerBtn.BackgroundColor3 = Color3.fromRGB(60, 40, 60); tpToPlayerBtn.Text = "Teleport Player"; tpToPlayerBtn.TextColor3 = Color3.new(1, 1, 1); tpToPlayerBtn.MouseButton1Click:Connect(function() if states.statsTarget then local tp = Players:FindFirstChild(states.statsTarget); if tp and tp.Character and tp.Character:FindFirstChild("HumanoidRootPart") and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then player.Character.HumanoidRootPart.CFrame = tp.Character.HumanoidRootPart.CFrame end end end)
local statNamesList = {"Strength", "Durability", "Agility", "Rebirth", "Rebirth Multiplier", "Brawl", "Kill", "Gems", "Brawl Kill", "Size", "Speed", "Good Karma", "Evil Karma", "Auto Lift", "Muscle King Time", "Save Size", "Save Speed", "Current Map"}
local statsDisp = {}; for _, s in ipairs(statNamesList) do local l = Instance.new("TextLabel", f3); l.Size = UDim2.new(1, -10, 0, 20); l.Text = s .. ": None"; l.BackgroundTransparency = 1; l.TextColor3 = Color3.new(1, 1, 1); l.TextXAlignment = Enum.TextXAlignment.Left; statsDisp[s] = l end
createToggle(f3, "View Player", "viewing", function(val) states.viewing = val; if not val and player.Character and player.Character:FindFirstChild("Humanoid") then workspace.CurrentCamera.CameraSubject = player.Character.Humanoid end end)
local invHeader = Instance.new("TextLabel", f3); invHeader.Size = UDim2.new(1, -10, 0, 25); invHeader.Text = "--- PETS ---"; invHeader.BackgroundTransparency = 1; invHeader.TextColor3 = Color3.fromRGB(200, 200, 0); invHeader.Font = Enum.Font.SourceSansBold; invHeader.TextSize = 18
petDetails = Instance.new("TextLabel", f3); petDetails.Size = UDim2.new(1, -10, 0, 140); petDetails.BackgroundTransparency = 1; petDetails.TextColor3 = Color3.new(1, 1, 1); petDetails.TextXAlignment = Enum.TextXAlignment.Left; petDetails.TextYAlignment = Enum.TextYAlignment.Top; petDetails.Text = "Select a Pet to view stats"

local function getTargetFolder(isPet, tableType)
local tp = Players:FindFirstChild(states.statsTarget); if not tp then return nil end
if isPet then local pf = tp:FindFirstChild("petsFolder"); return pf and pf:FindFirstChild(tableType)
else local puf = tp:FindFirstChild("powerUpsFolder"); return puf and puf:FindFirstChild(tableType) end
end

basicDD = createDropdown(f3, "Basic Pets", {"Nothing Here"}, false, function(l) states.currentPetLabel = l; states.currentPetTable = "Basic"; handleItemSelection(l, petRefsBasic, petDetails, getTargetFolder(true, "Basic"), basicDD) end)
advDD = createDropdown(f3, "Advance Pets", {"Nothing Here"}, false, function(l) states.currentPetLabel = l; states.currentPetTable = "Advanced"; handleItemSelection(l, petRefsAdv, petDetails, getTargetFolder(true, "Advanced"), advDD) end)
rareDD = createDropdown(f3, "Rare Pets", {"Nothing Here"}, false, function(l) states.currentPetLabel = l; states.currentPetTable = "Rare"; handleItemSelection(l, petRefsRare, petDetails, getTargetFolder(true, "Rare"), rareDD) end)
epicDD = createDropdown(f3, "Epic Pets", {"Nothing Here"}, false, function(l) states.currentPetLabel = l; states.currentPetTable = "Epic"; handleItemSelection(l, petRefsEpic, petDetails, getTargetFolder(true, "Epic"), epicDD) end)
uniqueDD = createDropdown(f3, "Unique Pets", {"Nothing Here"}, false, function(l) states.currentPetLabel = l; states.currentPetTable = "Unique"; handleItemSelection(l, petRefsUnique, petDetails, getTargetFolder(true, "Unique"), uniqueDD) end)

local auraHeader = Instance.new("TextLabel", f3); auraHeader.Size = UDim2.new(1, -10, 0, 25); auraHeader.Text = "--- AURAS ---"; auraHeader.BackgroundTransparency = 1; auraHeader.TextColor3 = Color3.fromRGB(200, 0, 200); auraHeader.Font = Enum.Font.SourceSansBold; auraHeader.TextSize = 18
auraDetails = Instance.new("TextLabel", f3); auraDetails.Size = UDim2.new(1, -10, 0, 140); auraDetails.BackgroundTransparency = 1; auraDetails.TextColor3 = Color3.new(1, 1, 1); auraDetails.TextXAlignment = Enum.TextXAlignment.Left; auraDetails.TextYAlignment = Enum.TextYAlignment.Top; auraDetails.Text = "Select an Aura to view stats"

auraBasicDD = createDropdown(f3, "Basic Auras", {"Nothing Here"}, false, function(l) states.currentAuraLabel = l; states.currentAuraTable = "Basic"; handleItemSelection(l, auraRefsBasic, auraDetails, getTargetFolder(false, "Basic"), auraBasicDD) end)
auraAdvDD = createDropdown(f3, "Advance Auras", {"Nothing Here"}, false, function(l) states.currentAuraLabel = l; states.currentAuraTable = "Advanced"; handleItemSelection(l, auraRefsAdv, auraDetails, getTargetFolder(false, "Advanced"), auraAdvDD) end)
auraRareDD = createDropdown(f3, "Rare Auras", {"Nothing Here"}, false, function(l) states.currentAuraLabel = l; states.currentAuraTable = "Rare"; handleItemSelection(l, auraRefsRare, auraDetails, getTargetFolder(false, "Rare"), auraRareDD) end)
auraEpicDD = createDropdown(f3, "Epic Auras", {"Nothing Here"}, false, function(l) states.currentAuraLabel = l; states.currentAuraTable = "Epic"; handleItemSelection(l, auraRefsEpic, auraDetails, getTargetFolder(false, "Epic"), auraEpicDD) end)
auraUniqueDD = createDropdown(f3, "Unique Auras", {"Nothing Here"}, false, function(l) states.currentAuraLabel = l; states.currentAuraTable = "Unique"; handleItemSelection(l, auraRefsUnique, auraDetails, getTargetFolder(false, "Unique"), auraUniqueDD) end)

local ultHeader = Instance.new("TextLabel", f3); ultHeader.Size = UDim2.new(1, -10, 0, 20); ultHeader.Text = "--- ULTIMATES ---"; ultHeader.BackgroundTransparency = 1; ultHeader.TextColor3 = Color3.fromRGB(0, 200, 200); ultHeader.Font = Enum.Font.SourceSansBold; ultHeader.TextXAlignment = Enum.TextXAlignment.Left
local ultList = Instance.new("TextLabel", f3); ultList.Size = UDim2.new(1, -10, 0, 250); ultList.BackgroundTransparency = 1; ultList.TextColor3 = Color3.new(1, 1, 1); ultList.TextXAlignment = Enum.TextXAlignment.Left; ultList.TextYAlignment = Enum.TextYAlignment.Top; ultList.Text = "None"; ultList.TextWrapped = true; ultList.AutomaticSize = Enum.AutomaticSize.Y
local gpHeader = Instance.new("TextLabel", f3); gpHeader.Size = UDim2.new(1, -10, 0, 20); gpHeader.Text = "--- GAMEPASSES ---"; gpHeader.BackgroundTransparency = 1; gpHeader.TextColor3 = Color3.fromRGB(0, 200, 100); gpHeader.Font = Enum.Font.SourceSansBold; gpHeader.TextXAlignment = Enum.TextXAlignment.Left
local gpList = Instance.new("TextLabel", f3); gpList.Size = UDim2.new(1, -10, 0, 200); gpList.BackgroundTransparency = 1; gpList.TextColor3 = Color3.new(1, 1, 1); gpList.TextXAlignment = Enum.TextXAlignment.Left; gpList.TextYAlignment = Enum.TextYAlignment.Top; gpList.Text = "None"; gpList.TextWrapped = true; gpList.AutomaticSize = Enum.AutomaticSize.Y
-- ==================== TAB 4-12 (Shortened for space, fully intact) ====================
local f4 = frames["Auto Gym"]; local mapDD; local gymDD
mapDD = createDropdown(f4, "Select Map", {"Tiny Island", "Beach", "Legend Beach", "Frozen Island", "Mystic Island", "Inferno Island", "Legend Island", "Muscle King Island", "Jungle Island"}, false, function(map) states.selectedMap = map; states.selectedGym = nil; local gymOptions = {}; local data = gymData[map]; if data then for name, info in pairs(data) do local costStr = info.cost and (" (" .. formatNum(info.cost) .. " Str)") or ""; table.insert(gymOptions, name .. costStr) end; if #gymOptions == 0 then table.insert(gymOptions, "No gyms available") end else table.insert(gymOptions, "No gyms available") end; gymDD.refresh(gymOptions); gymDD.setHeaderText("Select Gym v") end)
ui.dropdownSetters["selectMap"] = mapDD; ui.dropdownHeaderSetters["selectMap"] = mapDD
gymDD = createDropdown(f4, "Select Gym", {"Select map first"}, false, function(gymName) if not states.selectedMap or gymName == "Select map first" or gymName == "No gyms available" then return end; states.selectedGym = gymName:gsub(" %(.*%)", "") end)
ui.dropdownSetters["selectGym"] = gymDD; ui.dropdownHeaderSetters["selectGym"] = gymDD
local gymBtn = Instance.new("TextButton", f4); gymBtn.Size = UDim2.new(1, -10, 0, 35); gymBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 100); gymBtn.Text = "Gym"; gymBtn.TextColor3 = Color3.new(1, 1, 1)
gymBtn.MouseButton1Click:Connect(function() if not states.selectedMap or not states.selectedGym then return end; local gym = gymData[states.selectedMap][states.selectedGym]; if not gym then return end; local cost = gym.cost; if cost and getStrength() < cost then return end; if states.selectedMap == "Muscle King Island" and not isLocalPlayerMuscleKing() and not isMuscleKingNone() then return end; local chr = player.Character; if not chr or not chr:FindFirstChild("HumanoidRootPart") then return end; local hum = chr:FindFirstChild("Humanoid"); if hum and hum.Sit then return end; chr.HumanoidRootPart.CFrame = CFrame.new(gym.pos + Vector3.new(0, 4, 0)); chr.HumanoidRootPart.AssemblyLinearVelocity = Vector3.zero; task.spawn(function() while hum and not hum.Sit do if states.stopEverything then break end; local seat = findBestSeat(gym.pos); if seat then fireMachineInteract(seat) end; task.wait() end end) end)
ui.autoGymBtn = createToggle(f4, "Auto Gym", "autoGym", function(val) if val then if not states.selectedMap or not states.selectedGym then return false end; startAutoGym() else stopAutoGym() end; return val end)

local f5 = frames["Auto Treadmill"]; local treadmillOrder = {"Tiny Treadmill","Small Treadmill","Medium Treadmill","Large Treadmill","Big Treadmill","Mystic Treadmill","Legend Treadmill","Jungle Treadmill"}; local treadmillOptions = {}; for _, name in ipairs(treadmillOrder) do local data = treadmillData[name]; if data then table.insert(treadmillOptions, name .. " (+" .. data.exp .. " EXP)") end end
ui.treadmillDD = createDropdown(f5, "Select Treadmill", treadmillOptions, false, function(display) local realName = display:match("^(.-) %("); if realName and treadmillData[realName] then states.selectedTreadmill = realName else states.selectedTreadmill = nil end end)
ui.autoTreadmillBtn = createToggle(f5, "Auto Treadmill", "autoTreadmill", function(val) if val then if not states.selectedTreadmill then return false end; startAutoTreadmill() else stopAutoTreadmill() end; return val end)

local f6 = frames["Auto Crystal"]; local petList = {}; local auraList = {}
local function scanCrystalShop()
local folder = ReplicatedStorage:FindFirstChild("cPetShopFolder"); if not folder then return end; local pets = {}; local auras = {}
for _, child in ipairs(folder:GetChildren()) do local priceFolder = child:FindFirstChild("priceValue"); if not priceFolder then continue end; local priceTypeObj = priceFolder:FindFirstChild("priceType"); local priceType = priceTypeObj and priceTypeObj:IsA("StringValue") and priceTypeObj.Value or ""; local lowerType = priceType:lower():gsub("^%s*(.-)%s*$", "%1"); if lowerType == "tokens" or lowerType == "token" then continue end; local imageId = ""; local imageObj = child:FindFirstChild("imageValue"); if imageObj and imageObj:IsA("StringValue") then imageId = imageObj.Value end; local rarity = ""; local rarityObj = child:FindFirstChild("rarityValue"); if rarityObj and rarityObj:IsA("StringValue") then rarity = rarityObj.Value end; local cost = 0; local costType = ""; if priceFolder:IsA("NumberValue") or priceFolder:IsA("IntValue") then cost = priceFolder.Value end; if priceTypeObj and priceTypeObj:IsA("StringValue") then costType = priceTypeObj.Value end; local data = {name = child.Name, imageId = imageId, rarity = rarity, cost = cost, costType = costType, folderChild = child}; if child:FindFirstChild("isPowerUp") then table.insert(auras, data) else table.insert(pets, data) end end
local rarityOrder = {["Basic"] = 1, ["Advanced"] = 2, ["Rare"] = 3, ["Epic"] = 4, ["Unique"] = 5}; table.sort(pets, function(a, b) local orderA = rarityOrder[a.rarity] or 99; local orderB = rarityOrder[b.rarity] or 99; if orderA == orderB then if a.cost == b.cost then return a.name < b.name end; return a.cost < b.cost end; return orderA < orderB end); table.sort(auras, function(a, b) local orderA = rarityOrder[a.rarity] or 99; local orderB = rarityOrder[b.rarity] or 99; if orderA == orderB then if a.cost == b.cost then return a.name < b.name end; return a.cost < b.cost end; return orderA < orderB end); petList = pets; auraList = auras
end; scanCrystalShop()
local function evolveByName(itemName) if not itemName then return end; pcall(function() ReplicatedStorage.rEvents.petEvolveEvent:FireServer(itemName) end) end
local function createCrystalSection(parent, title, list, stateKeyData, stateKeyName, startAutoFunc, stopAutoFunc)
local titleLabel = Instance.new("TextLabel", parent); titleLabel.Size = UDim2.new(1, -10, 0, 25); titleLabel.BackgroundTransparency = 1; titleLabel.Text = title; titleLabel.TextColor3 = Color3.fromRGB(255, 215, 0); titleLabel.Font = Enum.Font.GothamBold; titleLabel.TextSize = 14; titleLabel.TextXAlignment = Enum.TextXAlignment.Left
local options = {}; local optionToData = {}; for _, data in ipairs(list) do local costStr = formatNum(data.cost); local display = data.name .. " | Cost: " .. costStr .. " | Rarity: " .. data.rarity; table.insert(options, display); optionToData[display] = data end; if #options == 0 then table.insert(options, "Nothing available") end
local imageLabel = Instance.new("ImageLabel", parent); imageLabel.Size = UDim2.new(1, -10, 0, 80); imageLabel.BackgroundTransparency = 1; imageLabel.Image = ""; imageLabel.ScaleType = Enum.ScaleType.Fit
local infoLabel = Instance.new("TextLabel", parent); infoLabel.Size = UDim2.new(1, -10, 0, 70); infoLabel.BackgroundColor3 = Color3.fromRGB(30, 30, 30); infoLabel.Text = "Select an item to view details\nCost: N/A"; infoLabel.TextColor3 = Color3.fromRGB(200, 200, 200); infoLabel.Font = Enum.Font.Gotham; infoLabel.TextSize = 11; infoLabel.TextWrapped = true; infoLabel.TextXAlignment = Enum.TextXAlignment.Left; infoLabel.TextYAlignment = Enum.TextYAlignment.Top; Instance.new("UICorner", infoLabel).CornerRadius = UDim.new(0, 8)
local dropdown = createDropdown(parent, title .. " Dropdown", options, false, function(display) if display == "Nothing available" then states[stateKeyData] = nil; states[stateKeyName] = nil; infoLabel.Text = "Select an item to view details\nCost: N/A"; imageLabel.Image = "" else local data = optionToData[display]; if data then states[stateKeyName] = display; states[stateKeyData] = data; local costStr = formatNum(data.cost); infoLabel.Text = "Name: " .. data.name .. "\nCost: " .. costStr .. " " .. data.costType .. "\nRarity: " .. data.rarity; if data.imageId ~= "" then imageLabel.Image = data.imageId else imageLabel.Image = "" end end end end)
local buyBtn = Instance.new("TextButton", parent); buyBtn.Size = UDim2.new(1, -10, 0, 35); buyBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 100); buyBtn.Text = "Buy"; buyBtn.TextColor3 = Color3.new(1, 1, 1); buyBtn.MouseButton1Click:Connect(function() local data = states[stateKeyData]; if not data then return end; fireBuyCrystal(data) end)
local buy5Btn = Instance.new("TextButton", parent); buy5Btn.Size = UDim2.new(1, -10, 0, 35); buy5Btn.BackgroundColor3 = Color3.fromRGB(60, 60, 100); buy5Btn.Text = "Buy 5"; buy5Btn.TextColor3 = Color3.new(1, 1, 1); buy5Btn.MouseButton1Click:Connect(function() local data = states[stateKeyData]; if not data then return end; for _ = 1, 5 do fireBuyCrystal(data) end end)
local buy5EvolveBtn = Instance.new("TextButton", parent); buy5EvolveBtn.Size = UDim2.new(1, -10, 0, 35); buy5EvolveBtn.BackgroundColor3 = Color3.fromRGB(100, 60, 40); buy5EvolveBtn.Text = "Buy 5 & Evolve"; buy5EvolveBtn.TextColor3 = Color3.new(1, 1, 1); buy5EvolveBtn.MouseButton1Click:Connect(function() local data = states[stateKeyData]; if not data then return end; for _ = 1, 5 do fireBuyCrystal(data) end; evolveByName(data.name); infoLabel.Text = "Bought 5 and evolved " .. data.name end)
local autoBuyToggle = createToggle(parent, "Auto Buy", nil, function(on) if on then local data = states[stateKeyData]; if not data then return false end; startAutoFunc() else stopAutoFunc() end; return on end)
return dropdown end
createCrystalSection(f6, "--- PETS ---", petList, "selectedPetCrystalData", "selectedPetCrystal", startAutoPetBuy, stopAutoPetBuy)
createCrystalSection(f6, "--- AURAS ---", auraList, "selectedAuraCrystalData", "selectedAuraCrystal", startAutoAuraBuy, stopAutoAuraBuy)

local f7 = frames["Glitchable Rebirth"]; local glitchRockOptions = {}; local rockOrder = {"Tiny Rock", "Punching Rock", "Large Rock", "Golden Rock", "Frozen Rock", "Mystic Rock", "Inferno Rock", "Legend Rock", "Muscle King Rock", "Jungle Rock"}; for _, rockName in ipairs(rockOrder) do local data = rockData[rockName]; if data then table.insert(glitchRockOptions, rockName .. " (Glitch Value: " .. data.glitchValue .. ")") end end; local selectedGlitchRock = nil; local expGivesResult = nil
ui.glitchRockDD = createDropdown(f7, "Rock Dropdown", glitchRockOptions, false, function(v) local rockName = v:match("^(.-) %("); if rockName and rockData[rockName] then selectedGlitchRock = rockName end; expGivesResult = nil; _G.GlitchExpGivesLabel.Text = "EXP GIVES: N/A"; _G.GlitchGlitchableLabel.Text = "GLITCHABLE?: N/A"; _G.GlitchRebirthRequiredLabel.Text = "Rebirth Required: N/A"; _G.GlitchPetExpCalcLabel.Text = "Result: N/A" end)
local expGivesLabel = Instance.new("TextLabel", f7); expGivesLabel.Size = UDim2.new(1, -10, 0, 25); expGivesLabel.BackgroundTransparency = 1; expGivesLabel.Text = "EXP GIVES: N/A"; expGivesLabel.TextColor3 = Color3.new(1, 1, 1); expGivesLabel.Font = Enum.Font.Gotham; expGivesLabel.TextSize = 12; _G.GlitchExpGivesLabel = expGivesLabel
local glitchableLabel = Instance.new("TextLabel", f7); glitchableLabel.Size = UDim2.new(1, -10, 0, 25); glitchableLabel.BackgroundTransparency = 1; glitchableLabel.Text = "GLITCHABLE?: N/A"; glitchableLabel.TextColor3 = Color3.new(1, 1, 1); glitchableLabel.Font = Enum.Font.Gotham; glitchableLabel.TextSize = 12; _G.GlitchGlitchableLabel = glitchableLabel
local calcExpGivesBtn = Instance.new("TextButton", f7); calcExpGivesBtn.Size = UDim2.new(1, -10, 0, 35); calcExpGivesBtn.BackgroundColor3 = Color3.fromRGB(40, 80, 140); calcExpGivesBtn.Text = "Calculate My Rebirth Exp Gives"; calcExpGivesBtn.TextColor3 = Color3.new(1, 1, 1); calcExpGivesBtn.MouseButton1Click:Connect(function() if not selectedGlitchRock then return end; local rock = rockData[selectedGlitchRock]; if not rock then return end; local rebirths = player.leaderstats and player.leaderstats:FindFirstChild("Rebirths") and player.leaderstats.Rebirths.Value or 0; local result = (rebirths + 20) * rock.glitchValue; expGivesResult = result; expGivesLabel.Text = "EXP GIVES: " .. result; glitchableLabel.Text = "GLITCHABLE?: " .. (result % 1 == 0 and "YES" or "NO") end)
local expTextBox = Instance.new("TextBox", f7); expTextBox.Size = UDim2.new(1, -10, 0, 30); expTextBox.PlaceholderText = "EXP (whole numbers only)"; expTextBox.BackgroundColor3 = Color3.fromRGB(40, 40, 40); expTextBox.TextColor3 = Color3.new(1, 1, 1); expTextBox.Text = ""; expTextBox:GetPropertyChangedSignal("Text"):Connect(function() local filtered = expTextBox.Text:gsub("[^%d]", ""); if filtered ~= expTextBox.Text then expTextBox.Text = filtered end end)
local calcRebirthRequiredBtn = Instance.new("TextButton", f7); calcRebirthRequiredBtn.Size = UDim2.new(1, -10, 0, 35); calcRebirthRequiredBtn.BackgroundColor3 = Color3.fromRGB(40, 80, 140); calcRebirthRequiredBtn.Text = "Check Rebirth Required"; calcRebirthRequiredBtn.TextColor3 = Color3.new(1, 1, 1); calcRebirthRequiredBtn.MouseButton1Click:Connect(function() if not selectedGlitchRock then return end; local rock = rockData[selectedGlitchRock]; if not rock then return end; local expValue = tonumber(expTextBox.Text); if not expValue then return end; local result = (expValue / rock.glitchValue) - 20; if result % 1 ~= 0 then _G.GlitchRebirthRequiredLabel.Text = "Rebirth Required: DECIMAL NUMBER DETECTED NO RESULT OUT" else _G.GlitchRebirthRequiredLabel.Text = "Rebirth Required: " .. math.floor(result) end end)
local rebirthRequiredLabel = Instance.new("TextLabel", f7); rebirthRequiredLabel.Size = UDim2.new(1, -10, 0, 25); rebirthRequiredLabel.BackgroundTransparency = 1; rebirthRequiredLabel.Text = "Rebirth Required: N/A"; rebirthRequiredLabel.TextColor3 = Color3.new(1, 1, 1); rebirthRequiredLabel.Font = Enum.Font.Gotham; rebirthRequiredLabel.TextSize = 12; _G.GlitchRebirthRequiredLabel = rebirthRequiredLabel
local petExpTextBox = Instance.new("TextBox", f7); petExpTextBox.Size = UDim2.new(1, -10, 0, 30); petExpTextBox.PlaceholderText = "PET EXP (whole numbers only)"; petExpTextBox.BackgroundColor3 = Color3.fromRGB(40, 40, 40); petExpTextBox.TextColor3 = Color3.new(1, 1, 1); petExpTextBox.Text = ""; petExpTextBox:GetPropertyChangedSignal("Text"):Connect(function() local filtered = petExpTextBox.Text:gsub("[^%d]", ""); if filtered ~= petExpTextBox.Text then petExpTextBox.Text = filtered end end)
local calcPetExpBtn = Instance.new("TextButton", f7); calcPetExpBtn.Size = UDim2.new(1, -10, 0, 35); calcPetExpBtn.BackgroundColor3 = Color3.fromRGB(40, 80, 140); calcPetExpBtn.Text = "Calculate PET EXP - EXP GIVES"; calcPetExpBtn.TextColor3 = Color3.new(1, 1, 1); calcPetExpBtn.MouseButton1Click:Connect(function() if not selectedGlitchRock then return end; if expGivesResult == nil then return end; local petExp = tonumber(petExpTextBox.Text); if not petExp then return end; local result = petExp - expGivesResult; _G.GlitchPetExpCalcLabel.Text = "Result: " .. result end)
local petExpCalcLabel = Instance.new("TextLabel", f7); petExpCalcLabel.Size = UDim2.new(1, -10, 0, 25); petExpCalcLabel.BackgroundTransparency = 1; petExpCalcLabel.Text = "Result: N/A"; petExpCalcLabel.TextColor3 = Color3.new(1, 1, 1); petExpCalcLabel.Font = Enum.Font.Gotham; petExpCalcLabel.TextSize = 12; _G.GlitchPetExpCalcLabel = petExpCalcLabel

local f8 = frames["Calculator"]; local calcInput = Instance.new("TextBox", f8); calcInput.Size = UDim2.new(1, -10, 0, 35); calcInput.PlaceholderText = "Expression (e.g. 2*2 or (20+20)*12.5)"; calcInput.BackgroundColor3 = Color3.fromRGB(40, 40, 40); calcInput.TextColor3 = Color3.new(1, 1, 1); calcInput.Font = Enum.Font.Gotham; calcInput.TextSize = 14; calcInput.ClearTextOnFocus = false
local calcResultLabel = Instance.new("TextLabel", f8); calcResultLabel.Size = UDim2.new(1, -10, 0, 30); calcResultLabel.BackgroundTransparency = 1; calcResultLabel.Text = "Result: "; calcResultLabel.TextColor3 = Color3.new(1, 1, 1); calcResultLabel.Font = Enum.Font.GothamBold; calcResultLabel.TextSize = 16
local function isSafeExpression(expr) return expr:match("^[%d%+%-%*%/%(%)%. ]+$") ~= nil end
local calcBtn = Instance.new("TextButton", f8); calcBtn.Size = UDim2.new(1, -10, 0, 40); calcBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 100); calcBtn.Text = "Calculate"; calcBtn.TextColor3 = Color3.new(1, 1, 1); calcBtn.MouseButton1Click:Connect(function() local expr = calcInput.Text:gsub("%s", ""); expr = expr:gsub("=", ""); expr = expr:gsub("[xX]", "*"); if expr == "" then calcResultLabel.Text = "Result: (empty)"; return end; if not isSafeExpression(expr) then calcResultLabel.Text = "Result: Invalid characters (only numbers, + - * / ( ) . allowed)"; return end; local success, result = pcall(function() return loadstring("return " .. expr)() end); if success then calcResultLabel.Text = "Result: " .. tostring(result) else calcResultLabel.Text = "Result: Invalid expression" end end)
local clearCalcBtn = Instance.new("TextButton", f8); clearCalcBtn.Size = UDim2.new(1, -10, 0, 35); clearCalcBtn.BackgroundColor3 = Color3.fromRGB(90, 40, 40); clearCalcBtn.Text = "Clear Text"; clearCalcBtn.TextColor3 = Color3.new(1, 1, 1); clearCalcBtn.MouseButton1Click:Connect(function() calcInput.Text = ""; calcResultLabel.Text = "Result: " end)

local f9 = frames["Teleport"]; local teleportList = { {name = "Safe Spot", pos = Vector3.new(218.565002, 41.153748, -8129.369629)}, {name = "Tiny Island", pos = Vector3.new(-33.693745, 7.456870, 1892.807007)}, {name = "Beach", pos = Vector3.new(4.466469, 7.303733, 132.878891)}, {name = "Frozen Island", pos = Vector3.new(-2623.022217, 7.303728, -409.073334)}, {name = "Mystic Island", pos = Vector3.new(2250.778076, 7.303732, 1073.226685)}, {name = "Inferno Island", pos = Vector3.new(-6758.963867, 7.303741, -1284.918701)}, {name = "Legends Island", pos = Vector3.new(4603.281738, 991.481750, -3897.865723)}, {name = "Muscle King Island", pos = Vector3.new(-8625.929688, 17.153761, -5730.473145)}, {name = "Jungle Island", pos = Vector3.new(-8685.621094, 6.732735, 2392.326660)} }
for _, tpData in ipairs(teleportList) do local btn = Instance.new("TextButton", f9); btn.Size = UDim2.new(1, -10, 0, 35); btn.Text = tpData.name; btn.BackgroundColor3 = Color3.fromRGB(60, 60, 100); btn.TextColor3 = Color3.new(1, 1, 1); btn.MouseButton1Click:Connect(function() local char = player.Character; if char and char:FindFirstChild("HumanoidRootPart") then char.HumanoidRootPart.CFrame = CFrame.new(tpData.pos) end end) end

local f_leaderboard = frames["Leaderboard"]; local leaderboardTitle = Instance.new("TextLabel", f_leaderboard); leaderboardTitle.Size = UDim2.new(1, -10, 0, 25); leaderboardTitle.BackgroundTransparency = 1; leaderboardTitle.Text = "--- LEADERBOARD ---"; leaderboardTitle.TextColor3 = Color3.fromRGB(255, 215, 0); leaderboardTitle.Font = Enum.Font.GothamBold; leaderboardTitle.TextSize = 16; leaderboardTitle.TextXAlignment = Enum.TextXAlignment.Center
local checkStrengthBtn = Instance.new("TextButton", f_leaderboard); checkStrengthBtn.Size = UDim2.new(1, -10, 0, 40); checkStrengthBtn.BackgroundColor3 = Color3.fromRGB(60, 100, 60); checkStrengthBtn.Text = "Check All Players Strength"; checkStrengthBtn.TextColor3 = Color3.new(1, 1, 1); checkStrengthBtn.Font = Enum.Font.GothamBold; checkStrengthBtn.TextSize = 14; checkStrengthBtn.MouseButton1Click:Connect(function() checkAllPlayersStrength(); updateLeaderboardDisplay() end)
local leaderboardScrollingFrame = Instance.new("ScrollingFrame", f_leaderboard); leaderboardScrollingFrame.Size = UDim2.new(1, -10, 0, 300); leaderboardScrollingFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 35); leaderboardScrollingFrame.ScrollBarThickness = isMobile and 6 or 4; leaderboardScrollingFrame.ScrollingEnabled = true; leaderboardScrollingFrame.ClipsDescendants = true; leaderboardScrollingFrame.ElasticBehavior = Enum.ElasticBehavior.WhenScrollable; leaderboardScrollingFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
local leaderboardLayout = Instance.new("UIListLayout", leaderboardScrollingFrame); leaderboardLayout.Padding = UDim.new(0, 2); leaderboardLayout.SortOrder = Enum.SortOrder.LayoutOrder
local function updateLeaderboardDisplay()
for _, child in ipairs(leaderboardScrollingFrame:GetChildren()) do if child:IsA("TextLabel") or child:IsA("Frame") then child:Destroy() end end
local data = states.leaderboardData; if not data or #data == 0 then local noDataLabel = Instance.new("TextLabel", leaderboardScrollingFrame); noDataLabel.Size = UDim2.new(1, 0, 0, 30); noDataLabel.BackgroundTransparency = 1; noDataLabel.Text = "No data available. Click 'Check All Players Strength'"; noDataLabel.TextColor3 = Color3.fromRGB(200, 200, 200); noDataLabel.TextSize = 12; leaderboardScrollingFrame.CanvasSize = UDim2.new(0, 0, 0, 35); return end
local rank = 1; for _, entry in ipairs(data) do local isLocalPlayer = entry.name == player.DisplayName .. " (@" .. player.Name .. "); local entryFrame = Instance.new("Frame", leaderboardScrollingFrame); entryFrame.Size = UDim2.new(1, -4, 0, 45); entryFrame.BackgroundColor3 = isLocalPlayer and Color3.fromRGB(60, 80, 40) or Color3.fromRGB(40, 40, 40); entryFrame.BorderSizePixel = 0; Instance.new("UICorner", entryFrame).CornerRadius = UDim.new(0, 4)
local rankLabel = Instance.new("TextLabel", entryFrame); rankLabel.Size = UDim2.new(0, 40, 1, 0); rankLabel.Position = UDim2.fromOffset(5, 0); rankLabel.BackgroundTransparency = 1; rankLabel.Text = "#" .. rank; rankLabel.TextColor3 = Color3.fromRGB(255, 200, 50); rankLabel.Font = Enum.Font.GothamBold; rankLabel.TextSize = 14; rankLabel.TextXAlignment = Enum.TextXAlignment.Left
local nameLabel = Instance.new("TextLabel", entryFrame); nameLabel.Size = UDim2.new(0, 180, 0, 20); nameLabel.Position = UDim2.fromOffset(50, 2); nameLabel.BackgroundTransparency = 1; nameLabel.Text = entry.name; nameLabel.TextColor3 = isLocalPlayer and Color3.new(0, 255, 100) or Color3.new(1, 1, 1); nameLabel.Font = Enum.Font.Gotham; nameLabel.TextSize = 11; nameLabel.TextXAlignment = Enum.TextXAlignment.Left; nameLabel.TextTruncate = Enum.TextTruncate.AtEnd
local rebirthDisplay = entry.rebirth >= 1000000 and formatNum(entry.rebirth) or tostring(math.floor(entry.rebirth))
local strengthLabel = Instance.new("TextLabel", entryFrame); strengthLabel.Size = UDim2.new(1, -55, 0, 20); strengthLabel.Position = UDim2.fromOffset(50, 22); strengthLabel.BackgroundTransparency = 1; strengthLabel.Text = "Strength: " .. formatNum(entry.strength) .. " | Rebirth: " .. rebirthDisplay .. " | Durability: " .. formatNum(entry.durability); strengthLabel.TextColor3 = Color3.fromRGB(200, 200, 200); strengthLabel.Font = Enum.Font.Gotham; strengthLabel.TextSize = 10; strengthLabel.TextXAlignment = Enum.TextXAlignment.Left; rank = rank + 1 end
leaderboardScrollingFrame.CanvasSize = UDim2.new(0, 0, 0, #data * 47 + 5)
end
table.insert(getgenv().ML_Expert_Loops, task.spawn(function() while true do task.wait(10); if states.stopEverything then continue end; if frames["Leaderboard"].Visible and #states.leaderboardData > 0 then checkAllPlayersStrength(); updateLeaderboardDisplay() end end end))

local f10 = frames["Config"]; local configFilePath = "ML_Expert_Configs.json"; local savedConfigs = {}; if isfile(configFilePath) then pcall(function() savedConfigs = HttpService:JSONDecode(readfile(configFilePath)); if type(savedConfigs) ~= "table" then savedConfigs = {} end end) end; local selectedConfigName = nil; local configNames = {}; for name, _ in pairs(savedConfigs) do table.insert(configNames, name) end; if #configNames == 0 then table.insert(configNames, "No saved configs") end
ui.configDropdown = createDropdown(f10, "Saved Config", configNames, false, function(name) if name ~= "No saved configs" then selectedConfigName = name end end)
local nameTextBox = Instance.new("TextBox", f10); nameTextBox.Size = UDim2.new(1, -10, 0, 30); nameTextBox.PlaceholderText = "Config Name"; nameTextBox.BackgroundColor3 = Color3.fromRGB(40, 40, 40); nameTextBox.TextColor3 = Color3.new(1, 1, 1); nameTextBox.Text = ""
local function stopAllAutomations() for key, btn in pairs(ui.toggleButtons) do if SetToggle[btn] then SetToggle[btn](false) end end; stopAutoGym(); stopAutoTreadmill(); stopAutoPetBuy(); stopAutoAuraBuy() end
local saveBtn = Instance.new("TextButton", f10); saveBtn.Size = UDim2.new(1, -10, 0, 35); saveBtn.Text = "Save Config"; saveBtn.BackgroundColor3 = Color3.fromRGB(40, 120, 40); saveBtn.TextColor3 = Color3.new(1, 1, 1); saveBtn.MouseButton1Click:Connect(function() local name = nameTextBox.Text:gsub("^%s*(.-)%s*$", "%1"); if name == "" then return end; local totalGlitchSeconds = states.glitchAccumulated; if states.autoGlitch then totalGlitchSeconds = totalGlitchSeconds + (os.time() - states.glitchStartTime) end; local preset = {config = {SizeValue = sizeBox.Text, WalkSpeed = wsBox.Text, FlySpeed = flySpeedBox.Text, RebirthTarget = rebBox.Text}, states = {selectedRock = states.selectedRock, selectedMethod = states.selectedMethod, autoGlitch = states.autoGlitch, glitchTotal = totalGlitchSeconds, autoGlitchTrain = states.autoGlitchTrain, autoLiftNoAnim = states.autoLiftNoAnim, autoLiftValue = states.autoLiftValue, autoRebirthTarget = states.autoRebirthTarget, autoRebirthInf = states.autoRebirthInf, selectedTool = states.selectedTool, autoEquip = states.autoEquip, autoClaim = states.autoClaim, autoFortuneWheel = states.autoFortuneWheel, fastPunch = states.fastPunch, autoPunch = states.autoPunch, accelReps = states.accelReps, autoSize = states.autoSize, autoWalkSpeed = states.autoWalkSpeed, fly = states.fly, godMode = states.godMode, lessLag = states.lessLag, autoMuscleKing = states.autoMuscleKing, selectedMap = states.selectedMap, selectedGym = states.selectedGym, autoGym = states.autoGym, autoUnequipBoosts = states.autoUnequipBoosts, lockPosition = states.lockedPos ~= nil, walkOnWater = states.walkOnWater, selectedTreadmill = states.selectedTreadmill, autoTreadmill = states.autoTreadmill}}; savedConfigs[name] = preset; pcall(function() writefile(configFilePath, HttpService:JSONEncode(savedConfigs)) end); local newNames = {}; for n, _ in pairs(savedConfigs) do table.insert(newNames, n) end; if #newNames == 0 then table.insert(newNames, "No saved configs") end; ui.configDropdown.refresh(newNames); ui.configDropdown.setHeaderText("Saved Config v") end)
createToggle(f10, "Stop Everything", "stopEverything", function(val) states.stopEverything = val; return val end)
local loadBtn = Instance.new("TextButton", f10); loadBtn.Size = UDim2.new(1, -10, 0, 35); loadBtn.Text = "Load Config"; loadBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 120); loadBtn.TextColor3 = Color3.new(1, 1, 1); loadBtn.MouseButton1Click:Connect(function() if not selectedConfigName or not savedConfigs[selectedConfigName] then return end; stopAllAutomations(); local preset = savedConfigs[selectedConfigName]; if preset.config then if preset.config.SizeValue then sizeBox.Text = preset.config.SizeValue; Config.SizeValue = preset.config.SizeValue end; if preset.config.WalkSpeed then wsBox.Text = preset.config.WalkSpeed; Config.WalkSpeed = preset.config.WalkSpeed end; if preset.config.FlySpeed then flySpeedBox.Text = preset.config.FlySpeed; Config.FlySpeed = preset.config.FlySpeed end; if preset.config.RebirthTarget then rebBox.Text = preset.config.RebirthTarget; Config.RebirthTarget = preset.config.RebirthTarget end end; local s = preset.states; if s then for k, v in pairs(s) do if k ~= "glitchTotal" and states[k] ~= nil then states[k] = v end end; local savedGlitchTotal = s.glitchTotal or 0; states.glitchAccumulated = savedGlitchTotal; if s.autoGlitch then states.autoGlitch = true; states.glitchStartTime = os.time() else states.autoGlitch = false; states.glitchStartTime = 0 end; local toggleStateMap = {autoGlitch = "autoGlitch", autoGlitchTrain = "autoGlitchTrain", autoLiftNoAnim = "autoLiftNoAnim", autoLiftValue = "autoLiftValue", autoEquip = "autoEquip", autoRebirthTarget = "autoRebirthTarget", autoRebirthInf = "autoRebirthInf", autoMuscleKing = "autoMuscleKing", autoClaim = "autoClaim", autoPunch = "autoPunch", autoFortuneWheel = "autoFortuneWheel", accelReps = "accelReps", fastPunch = "fastPunch", lessLag = "lessLag", autoSize = "autoSize", autoWalkSpeed = "autoWalkSpeed", fly = "fly", godMode = "godMode", autoUnequipBoosts = "autoUnequipBoosts", autoGym = "autoGym", stopEverything = "stopEverything", trackStats = "trackStats", withoutFormatNum = "withoutFormatNum", viewing = "viewing", lockPosition = "lockPosition", walkOnWater = "walkOnWater", autoTreadmill = "autoTreadmill"}; for stateKey, toggleKey in pairs(toggleStateMap) do local btn = ui.toggleButtons[toggleKey]; if btn and SetToggle[btn] then local val = s[stateKey]; if val ~= nil then SetToggle[btn](val) end end end; if s.selectedRock and ui.dropdownSetters["selectRock"] then ui.dropdownSetters["selectRock"].setValue(s.selectedRock) end; if s.selectedMethod and ui.dropdownSetters["selectMethod"] then ui.dropdownSetters["selectMethod"].setValue(s.selectedMethod) end; if s.selectedTool and ui.dropdownSetters["toolDropdown"] then ui.dropdownSetters["toolDropdown"].setValue(s.selectedTool) end; if s.selectedMap and ui.dropdownSetters["selectMap"] then ui.dropdownSetters["selectMap"].setValue(s.selectedMap) end; if s.selectedGym and ui.dropdownSetters["selectGym"] and s.selectedMap then local mapData = gymData[s.selectedMap]; if mapData then local gymDisplayName = s.selectedGym; for name, info in pairs(mapData) do local costStr = info.cost and (" (" .. formatNum(info.cost) .. " Str)") or ""; local fullName = name .. costStr; if name == s.selectedGym or fullName == s.selectedGym then gymDisplayName = fullName; break end end; ui.dropdownSetters["selectGym"].setValue(gymDisplayName) end end; if s.selectedTreadmill and ui.treadmillDD then local display = s.selectedTreadmill .. " (+" .. treadmillData[s.selectedTreadmill].exp .. " EXP)"; ui.treadmillDD.setValue(display) end; if s.lockPosition and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then states.lockedPos = player.Character.HumanoidRootPart.CFrame end; if s.autoGym then startAutoGym() end; if s.autoTreadmill and states.selectedTreadmill then startAutoTreadmill() end end end)
local removeConfigBtn = Instance.new("TextButton", f10); removeConfigBtn.Size = UDim2.new(1, -10, 0, 35); removeConfigBtn.Text = "Remove Config"; removeConfigBtn.BackgroundColor3 = Color3.fromRGB(120, 40, 40); removeConfigBtn.TextColor3 = Color3.new(1, 1, 1); removeConfigBtn.MouseButton1Click:Connect(function() if not selectedConfigName or selectedConfigName == "No saved configs" or not savedConfigs[selectedConfigName] then return end; createConfirmPopup("Are you sure you want to delete config \"" .. selectedConfigName .. "\"?", function(confirmed) if confirmed then savedConfigs[selectedConfigName] = nil; pcall(function() writefile(configFilePath, HttpService:JSONEncode(savedConfigs)) end); local newNames = {}; for n, _ in pairs(savedConfigs) do table.insert(newNames, n) end; if #newNames == 0 then table.insert(newNames, "No saved configs") end; ui.configDropdown.refresh(newNames); selectedConfigName = nil; ui.configDropdown.setHeaderText("Saved Config v") end end) end)

-- ==================== TAB 12: KILLER ====================
local f_killer = frames["Killer"]
local function refreshKillerDropdowns()
local playerOptions = {}; local allPlayers = Players:GetPlayers(); local hasOthers = false
for _, p in ipairs(allPlayers) do if p ~= player then table.insert(playerOptions, getPlayerDisplayString(p)); hasOthers = true end end
if not hasOthers then table.insert(playerOptions, "No one") end
if ui.killerPlayerDD then ui.killerPlayerDD.refresh(playerOptions) end; if ui.killerWhitelistDD then ui.killerWhitelistDD.refresh(playerOptions) end
end
ui.killerPlayerDD = createDropdown(f_killer, "Player Dropdown", {}, true, function(display) if display == "No one" then return false end; local username = getUsernameFromDisplay(display); if not username then return false end; if killerState.selectedPlayers[username] then killerState.selectedPlayers[username] = nil; if killerState.killSelectedEnabled then killSources.selected[username] = nil end; return false else killerState.selectedPlayers[username] = true; if killerState.killSelectedEnabled then killSources.selected[username] = true end; return true end end, function(display) if display == "No one" then return false end; local username = getUsernameFromDisplay(display); return killerState.selectedPlayers[username] == true end)
ui.killerWhitelistDD = createDropdown(f_killer, "Whitelist Dropdown", {}, true, function(display) if display == "No one" then return false end; local username = getUsernameFromDisplay(display); if not username then return false end; if killerState.autoWhitelist[username] then return true end; if isWhitelisted(username) then killerState.manualWhitelist[username] = nil; if not killerState.autoWhitelist[username] then if killerState.killEveryoneEnabled then killSources.everyone[username] = nil end end; return false else killerState.manualWhitelist[username] = true; if killerState.killEveryoneEnabled then killSources.everyone[username] = nil end; return true end end, function(display) if display == "No one" then return false end; local username = getUsernameFromDisplay(display); return isWhitelisted(username) end)
ui.whitelistFriendsToggle = createToggle(f_killer, "Whitelist Friends", "whitelistFriends", function(val) killerState.whitelistFriendsEnabled = val; if val then for _, p in ipairs(Players:GetPlayers()) do if p ~= player then pcall(function() if Players:IsFriendsWith(p.UserId) then killerState.autoWhitelist[p.Name] = true; if killerState.killEveryoneEnabled then killSources.everyone[p.Name] = nil end end end) end end else for name in pairs(killerState.autoWhitelist) do killerState.autoWhitelist[name] = nil end end; refreshKillerDropdowns(); if killerState.killEveryoneEnabled then killSources.everyone = {}; for _, p in ipairs(Players:GetPlayers()) do if p ~= player and not isWhitelisted(p.Name) then killSources.everyone[p.Name] = true end end end; return val end)
ui.killToggle = createToggle(f_killer, "Kill", "killSelected", function(val) killerState.killSelectedEnabled = val; if val then for name in pairs(killerState.selectedPlayers) do killSources.selected[name] = true end else for name in pairs(killSources.selected) do killSources.selected[name] = nil end end; return val end)
ui.killEveryoneToggle = createToggle(f_killer, "Kill Everyone", "killEveryone", function(val) killerState.killEveryoneEnabled = val; if val then for _, p in ipairs(Players:GetPlayers()) do if p ~= player and not isWhitelisted(p.Name) then killSources.everyone[p.Name] = true end end else for name in pairs(killSources.everyone) do killSources.everyone[name] = nil end end; return val end)
ui.autoGoodKarmaToggle = createToggle(f_killer, "Auto Good Karma", "autoGoodKarma", function(val) killerState.autoGoodKarmaEnabled = val; if not val then for name in pairs(killSources.goodKarma) do killSources.goodKarma[name] = nil end end; return val end)
ui.autoEvilKarmaToggle = createToggle(f_killer, "Auto Evil Karma", "autoEvilKarma", function(val) killerState.autoEvilKarmaEnabled = val; if not val then for name in pairs(killSources.evilKarma) do killSources.evilKarma[name] = nil end end; return val end)

table.insert(getgenv().ML_Expert_Loops, task.spawn(function() while true do task.wait(0.05); if states.stopEverything then continue end; local punchTool = getPunch(); local hasPunch = punchTool and punchTool.Parent == player.Character; local effective = {}; for srcName, srcSet in pairs(killSources) do for name, _ in pairs(srcSet) do effective[name] = true end end; if not hasPunch then continue end; local myChar = player.Character; if not myChar then continue end; local myLeftHand = myChar:FindFirstChild("LeftHand") or myChar:FindFirstChild("Left Arm"); local myRightHand = myChar:FindFirstChild("RightHand") or myChar:FindFirstChild("Right Arm"); if not myLeftHand or not myRightHand then continue end; for targetName in pairs(effective) do local targetPlayer = Players:FindFirstChild(targetName); if not targetPlayer or not targetPlayer.Character then continue end; local char = targetPlayer.Character; local torso = findTorso(char); if not torso then continue end; if torso:IsA("BasePart") then if not originalTorsoSize[targetName] then originalTorsoSize[targetName] = torso.Size end; pcall(function() torso.Size = Vector3.new(1, 1, 1) end) end; local newAnchorList = {}; for _, part in ipairs(char:GetDescendants()) do if part:IsA("BasePart") and part ~= torso and part.Name ~= "Torso" and part.Name ~= "UpperTorso" and part.Name ~= "HumanoidRootPart" then if not part.Anchored then part.Anchored = true end; table.insert(newAnchorList, part) end end; local oldAnchorList = anchoredParts[targetName]; if oldAnchorList then for _, part in ipairs(oldAnchorList) do if not part.Parent or not table.find(newAnchorList, part) then pcall(function() part.Anchored = false end) end end; anchoredParts[targetName] = newAnchorList; local newAttachList = {}; for _, desc in ipairs(char:GetDescendants()) do if desc:IsA("Motor6D") or desc:IsA("Weld") or desc:IsA("WeldConstraint") then local part0, part1 = nil, nil; if desc:IsA("Motor6D") or desc:IsA("Weld") then part0 = desc.Part0; part1 = desc.Part1 elseif desc:IsA("WeldConstraint") then part0 = desc.Part0; part1 = desc.Part1 end; if part0 == torso or part1 == torso then continue end; if desc.Enabled ~= false then desc.Enabled = false end; table.insert(newAttachList, desc) end end; local oldAttachList = disabledAttachments[targetName]; if oldAttachList then for _, inst in ipairs(oldAttachList) do if not inst.Parent or not table.find(newAttachList, inst) then pcall(function() inst.Enabled = true end) end end; disabledAttachments[targetName] = newAttachList; local handName, targetHand; if killerToggleHand then handName = "leftHand"; targetHand = myLeftHand else handName = "rightHand"; targetHand = myRightHand end; killerToggleHand = not killerToggleHand; torso.CFrame = targetHand.CFrame; pcall(function() player.muscleEvent:FireServer("punch", handName) end) end end end))
table.insert(getgenv().ML_Expert_Loops, task.spawn(function() while true do task.wait(0.3); if states.stopEverything then continue end; local anyKillActive = killerState.killSelectedEnabled or killerState.killEveryoneEnabled or killerState.autoGoodKarmaEnabled or killerState.autoEvilKarmaEnabled; local punch = getPunch(); local hasPunch = punch and punch.Parent == player.Character; if not anyKillActive or not hasPunch then for _, list in pairs(disabledAttachments) do for _, inst in ipairs(list) do if inst.Parent then pcall(function() inst.Enabled = true end) end end; table.clear(disabledAttachments); for _, list in pairs(anchoredParts) do for _, part in ipairs(list) do if part.Parent then pcall(function() part.Anchored = false end) end end; table.clear(anchoredParts); for targetName, size in pairs(originalTorsoSize) do local targetPlayer = Players:FindFirstChild(targetName); if targetPlayer and targetPlayer.Character then local t = findTorso(targetPlayer.Character); if t then pcall(function() t.Size = size end) end end; table.clear(originalTorsoSize) end end end))
table.insert(getgenv().ML_Expert_Loops, task.spawn(function() while true do task.wait(1); if states.stopEverything then continue end; if killerState.autoGoodKarmaEnabled then for _, p in ipairs(Players:GetPlayers()) do if p ~= player then local good = p:FindFirstChild("goodKarma"); local evil = p:FindFirstChild("evilKarma"); local goodVal = good and good.Value or 0; local evilVal = evil and evil.Value or 0; if evilVal > goodVal and not isWhitelisted(p.Name) then killSources.goodKarma[p.Name] = true else killSources.goodKarma[p.Name] = nil end end end else for name in pairs(killSources.goodKarma) do killSources.goodKarma[name] = nil end end; if killerState.autoEvilKarmaEnabled then for _, p in ipairs(Players:GetPlayers()) do if p ~= player then local good = p:FindFirstChild("goodKarma"); local evil = p:FindFirstChild("evilKarma"); local goodVal = good and good.Value or 0; local evilVal = evil and evil.Value or 0; if goodVal > evilVal and not isWhitelisted(p.Name) then killSources.evilKarma[p.Name] = true else killSources.evilKarma[p.Name] = nil end end end else for name in pairs(killSources.evilKarma) do killSources.evilKarma[name] = nil end end end end))

Players.PlayerAdded:Connect(function(p) if p == player then return end; refreshKillerDropdowns(); if killerState.whitelistFriendsEnabled then pcall(function() if Players:IsFriendsWith(p.UserId) then killerState.autoWhitelist[p.Name] = true; if killerState.killEveryoneEnabled then killSources.everyone[p.Name] = nil end end end) end; if killerState.killEveryoneEnabled and not isWhitelisted(p.Name) then killSources.everyone[p.Name] = true end end)
Players.PlayerRemoving:Connect(function(p) local name = p.Name; killerState.selectedPlayers[name] = nil; killerState.manualWhitelist[name] = nil; killerState.autoWhitelist[name] = nil; killSources.selected[name] = nil; killSources.everyone[name] = nil; killSources.goodKarma[name] = nil; killSources.evilKarma[name] = nil; local attachList = disabledAttachments[name]; if attachList then for _, inst in ipairs(attachList) do if inst.Parent then pcall(function() inst.Enabled = true end) end end; disabledAttachments[name] = nil; local anchorList = anchoredParts[name]; if anchorList then for _, part in ipairs(anchorList) do if part.Parent then pcall(function() part.Anchored = false end) end end; anchoredParts[name] = nil; local torsoSize = originalTorsoSize[name]; if torsoSize then local char = p.Character; if char then local t = findTorso(char); if t then pcall(function() t.Size = torsoSize end) end end; originalTorsoSize[name] = nil; refreshKillerDropdowns() end)
refreshKillerDropdowns()

-- ==================== MAIN LOOPS ====================
table.insert(getgenv().ML_Expert_Loops, RunService.Heartbeat:Connect(function() if states.stopEverything then return end; if states.autoMuscleKing then local char = player.Character; if char and char:FindFirstChild("HumanoidRootPart") then local mkPos = Vector3.new(-8761.708984, 494.232361, -5858.251953); char.HumanoidRootPart.CFrame = CFrame.new(mkPos) end elseif states.lockedPos then local char = player.Character; if char and char:FindFirstChild("HumanoidRootPart") then char.HumanoidRootPart.CFrame = states.lockedPos end end end))
local function getTeleportPosForMap(currentMap) if not currentMap or currentMap == "" then return Vector3.new(4.466469, 7.303733, 132.878891) end; local lowerMap = currentMap:lower(); if lowerMap:find("jungle") then return Vector3.new(-8685.621094, 6.732735, 2392.326660) elseif lowerMap:find("muscle king") then return Vector3.new(-8625.929688, 17.153761, -5730.473145) elseif lowerMap:find("beach") then return Vector3.new(4.466469, 7.303733, 132.878891) elseif lowerMap:find("tiny") then return Vector3.new(-33.693745, 7.456870, 1892.807007) elseif lowerMap:find("frozen") then return Vector3.new(-2623.022217, 7.303728, -409.073334) elseif lowerMap:find("mystic") then return Vector3.new(2250.778076, 7.303732, 1073.226685) elseif lowerMap:find("inferno") then return Vector3.new(-6758.963867, 7.303741, -1284.918701) elseif lowerMap:find("legend") then return Vector3.new(4603.281738, 991.481750, -3897.865723) else return Vector3.new(4.466469, 7.303733, 132.878891) end end
table.insert(getgenv().ML_Expert_Loops, task.spawn(function() while true do task.wait(0.5); if states.stopEverything then continue end; local char = player.Character; if char and char:FindFirstChild("HumanoidRootPart") then local root = char.HumanoidRootPart; if root.Position.Y < -50 then local currentMap = player:FindFirstChild("currentMap") and player.currentMap.Value or ""; local teleportPos = getTeleportPosForMap(currentMap); pcall(function() root.CFrame = CFrame.new(teleportPos); root.AssemblyLinearVelocity = Vector3.zero end) end end end end))
table.insert(getgenv().ML_Expert_Loops, task.spawn(function() local waterPart = workspace:FindFirstChild("underSea"); if not waterPart or not waterPart:IsA("BasePart") then return end; local waterY = waterPart.Position.Y; while true do if not states.walkOnWater then if walkOnWaterPlatform then walkOnWaterPlatform:Destroy(); walkOnWaterPlatform = nil end; task.wait(0.5); continue end; if not walkOnWaterPlatform then walkOnWaterPlatform = Instance.new("Part"); walkOnWaterPlatform.Name = "ML_WalkOnWaterPlatform"; walkOnWaterPlatform.Size = Vector3.new(10, 1, 10); walkOnWaterPlatform.Anchored = true; walkOnWaterPlatform.CanCollide = true; walkOnWaterPlatform.Transparency = 1; walkOnWaterPlatform.Parent = workspace; local old = workspace:FindFirstChild("ML_WalkOnWaterPlatform"); if old and old ~= walkOnWaterPlatform then old:Destroy() end end; local char = player.Character; if char and char:FindFirstChild("HumanoidRootPart") then local root = char.HumanoidRootPart; walkOnWaterPlatform.CFrame = CFrame.new(root.Position.X, waterY, root.Position.Z) end; task.wait(0.05) end end))
table.insert(getgenv().ML_Expert_Loops, task.spawn(function() local allowedWorkoutTools = {["Punch"] = true, ["Weight"] = true, ["Stomp"] = true, ["Ground Slam"] = true, ["Pushups"] = true, ["Handstands"] = true, ["Situps"] = true}; while true do task.wait(); if states.stopEverything then continue end; if not states.autoUnequipBoosts then continue end; local char = player.Character; if not char then continue end; for _, tool in ipairs(char:GetChildren()) do if tool:IsA("Tool") and not allowedWorkoutTools[tool.Name] then local bp = player:FindFirstChild("Backpack"); if bp then pcall(function() tool.Parent = bp end) end end end end end))
table.insert(getgenv().ML_Expert_Loops, task.spawn(function() while true do if states.lessLag then task.wait(0.1) else RunService.Heartbeat:Wait() end; if states.stopEverything then continue end; if states.godMode then pcall(function() ReplicatedStorage.rEvents.brawlEvent:FireServer("joinBrawl") end) end; if states.autoSize then pcall(function() ReplicatedStorage.rEvents.changeSpeedSizeRemote:InvokeServer("changeSize", tonumber(Config.SizeValue) or 1) end) end; local char = player.Character; if char and char:FindFirstChild("HumanoidRootPart") and char:FindFirstChild("Humanoid") then local root, hum = char.HumanoidRootPart, char.Humanoid; if states.autoWalkSpeed then hum.WalkSpeed = tonumber(Config.WalkSpeed) or 16 end; if states.fly then hum.PlatformStand = true; if not states.flyBody or not states.flyBody.Velocity.Parent then states.flyBody = {}; local bv = Instance.new("BodyVelocity", root); bv.MaxForce, bv.Velocity = Vector3.new(math.huge, math.huge, math.huge), Vector3.zero; local bg = Instance.new("BodyGyro", root); bg.MaxTorque, bg.CFrame = Vector3.new(math.huge, math.huge, math.huge), root.CFrame; states.flyBody.Velocity, states.flyBody.Gyro = bv, bg end; local speed, cam = tonumber(Config.FlySpeed) or 50, workspace.CurrentCamera; if states.flyBody.Gyro.Parent and states.flyBody.Velocity.Parent then states.flyBody.Gyro.CFrame = cam.CFrame; local moveVec = Vector3.new(); if UserInputService:IsKeyDown(Enum.KeyCode.W) or UserInputService:IsKeyDown(Enum.KeyCode.Up) then moveVec = moveVec + Vector3.new(0, 0, -1) end; if UserInputService:IsKeyDown(Enum.KeyCode.S) or UserInputService:IsKeyDown(Enum.KeyCode.Down) then moveVec = moveVec + Vector3.new(0, 0, 1) end; if UserInputService:IsKeyDown(Enum.KeyCode.A) or UserInputService:IsKeyDown(Enum.KeyCode.Left) then moveVec = moveVec + Vector3.new(-1, 0, 0) end; if UserInputService:IsKeyDown(Enum.KeyCode.D) or UserInputService:IsKeyDown(Enum.KeyCode.Right) then moveVec = moveVec + Vector3.new(1, 0, 0) end; if moveVec.Magnitude == 0 then pcall(function() local controlModule = require(player.PlayerScripts:WaitForChild("PlayerModule"):WaitForChild("ControlModule")); moveVec = controlModule:GetMoveVector() end) end; if moveVec and moveVec.Magnitude > 0 then states.flyBody.Velocity.Velocity = (cam.CFrame.RightVector * moveVec.X + cam.CFrame.LookVector * -moveVec.Z) * speed else states.flyBody.Velocity.Velocity = Vector3.zero end end end end end end))
table.insert(getgenv().ML_Expert_Loops, task.spawn(function() while true do task.wait(); if states.stopEverything then continue end; local char = player.Character; if not char then continue end; local tool = char:FindFirstChild("Weight"); local rightHand = char:FindFirstChild("RightHand") or char:FindFirstChild("Right Arm"); if tool and rightHand then local handle = tool:FindFirstChild("Handle"); if handle then handle.Anchored = false; local glue = handle:FindFirstChild("WeightGlue"); if not glue then pcall(function() local newGlue = Instance.new("WeldConstraint"); newGlue.Name = "WeightGlue"; newGlue.Part0 = rightHand; newGlue.Part1 = handle; newGlue.Parent = handle end) end end else local bp = player:FindFirstChild("Backpack"); if bp then for _, t in ipairs(bp:GetChildren()) do if t:IsA("Tool") and t.Name == "Weight" then local h = t:FindFirstChild("Handle"); if h then local g = h:FindFirstChild("WeightGlue"); if g then g:Destroy() end end end end end end end end))
table.insert(getgenv().ML_Expert_Loops, task.spawn(function() while true do if states.lessLag then task.wait(0.1) else RunService.Heartbeat:Wait() end; if states.stopEverything then continue end; if not states.autoEquip then task.wait(); continue end; local bp = player:FindFirstChild("Backpack"); local char = player.Character; if not bp or not char then task.wait(0.2); continue end; local hum = char:FindFirstChild("Humanoid"); if not hum or hum.Health <= 0 then task.wait(0.2); continue end; local targetTool = nil; if states.selectedTool and states.selectedTool ~= "NO TOOL FOUND" then targetTool = bp:FindFirstChild(states.selectedTool) or char:FindFirstChild(states.selectedTool) end; if not targetTool then targetTool = bp:FindFirstChild("Weight") or char:FindFirstChild("Weight") end; if targetTool and targetTool.Parent ~= char then pcall(function() hum:EquipTool(targetTool) end) end; local keepToolName = targetTool and targetTool.Name or "Weight"; for _, t in ipairs(char:GetChildren()) do if t:IsA("Tool") and t.Name ~= keepToolName and t.Name ~= "Punch" then pcall(function() t.Parent = bp end) end end; task.wait() end end))
table.insert(getgenv().ML_Expert_Loops, task.spawn(function() while true do task.wait(); if states.stopEverything then continue end; if not states.autoGlitch then continue end; local char = player.Character; if not char or not states.selectedRock or not states.selectedMethod then continue end; local root, hum = char:FindFirstChild("HumanoidRootPart"), char:FindFirstChild("Humanoid"); if not root or not hum or hum.Health <= 0 then continue end; if states.isTransitioning then continue end; local rockModel = getRockInstance(states.selectedRock); if not rockModel then continue end; local actualRockPart = rockModel:FindFirstChild("Rock") or rockModel:FindFirstChildWhichIsA("BasePart"); if not actualRockPart then continue end; local punchTool = getPunch(); if punchTool and punchTool.Parent ~= char then pcall(function() punchTool.Parent = char end) end; if states.selectedMethod == "Teleport" then if punchTool and punchTool:FindFirstChild("attackTime") then punchTool.attackTime.Value = 0.05 end; local edgeOffset = (actualRockPart.Size.Z / 2) + 2.5 + ((tonumber(Config.SizeValue) or 1) * 0.25); root.CFrame = CFrame.lookAt((actualRockPart.CFrame * CFrame.new(0, 0, edgeOffset)).Position, actualRockPart.Position); if punchTool then punchTool:Activate() end elseif states.selectedMethod == "NonTeleport" then local currentHand = toggleHand and "Left" or "Right"; local targetHand = char:FindFirstChild(currentHand .. "Hand") or char:FindFirstChild(currentHand .. " Arm"); toggleHand = not toggleHand; if targetHand then if not rockCache[actualRockPart] then rockCache[actualRockPart] = {Size = actualRockPart.Size, CanCollide = actualRockPart.CanCollide, CFrame = actualRockPart.CFrame, Anchored = actualRockPart.Anchored} end; actualRockPart.CanCollide = false; actualRockPart.Anchored = false; local mySize = player:FindFirstChild("customSize") and player.customSize.Value or tonumber(Config.SizeValue) or 1; local newSize = math.min(2, math.max(0.5, mySize / 2)); actualRockPart.Size = Vector3.new(newSize, newSize, newSize); actualRockPart.CFrame = targetHand.CFrame; actualRockPart.AssemblyLinearVelocity = Vector3.zero; actualRockPart.AssemblyAngularVelocity = Vector3.zero end; local handToFire = toggleHand and "leftHand" or "rightHand"; pcall(function() player.muscleEvent:FireServer("punch", handToFire) end) end; if not states.autoGlitchTrain then for _, tool in ipairs(char:GetChildren()) do if tool:IsA("Tool") and tool.Name ~= "Punch" then local bp = player:FindFirstChild("Backpack"); if bp then pcall(function() tool.Parent = bp end) end end end end end end))
table.insert(getgenv().ML_Expert_Loops, task.spawn(function() while true do if states.lessLag then task.wait(0.1) else RunService.Heartbeat:Wait() end; if states.stopEverything then continue end; if states.autoLiftNoAnim then pcall(function() player.muscleEvent:FireServer("rep") end) end end end))
table.insert(getgenv().ML_Expert_Loops, task.spawn(function() while task.wait(1) do if states.stopEverything then continue end; if states.autoClaim then for i=1,8 do pcall(function() ReplicatedStorage.rEvents.freeGiftClaimRemote:InvokeServer("claimGift", i) end) end end end end))
table.insert(getgenv().ML_Expert_Loops, task.spawn(function() while task.wait(1) do if states.stopEverything then continue end; if states.autoFortuneWheel then pcall(function() local args = {[1] = "openFortuneWheel", [2] = game:GetService("ReplicatedStorage").fortuneWheelChances:FindFirstChild("Fortune Wheel")}; game:GetService("ReplicatedStorage").rEvents.openFortuneWheelRemote:InvokeServer(unpack(args)) end) end end end end))
table.insert(getgenv().ML_Expert_Loops, task.spawn(function() while task.wait() do if states.stopEverything then continue end; if states.autoRebirthInf then ReplicatedStorage.rEvents.rebirthRemote:InvokeServer("rebirthRequest") end; if states.autoRebirthTarget then local target = tonumber(Config.RebirthTarget) or 0; if player.leaderstats.Rebirths.Value < target then ReplicatedStorage.rEvents.rebirthRemote:InvokeServer("rebirthRequest") else states.autoRebirthTarget = false; if ui.toggleButtons["autoRebirthTarget"] then SetToggle[ui.toggleButtons["autoRebirthTarget"]](false) end end end; if states.viewing and states.statsTarget then local tp = Players:FindFirstChild(states.statsTarget); if tp and tp.Character and tp.Character:FindFirstChild("Humanoid") then workspace.CurrentCamera.CameraSubject = tp.Character.Humanoid end elseif not states.viewing and player.Character and player.Character:FindFirstChild("Humanoid") then workspace.CurrentCamera.CameraSubject = player.Character.Humanoid end; if states.trackStats and states.statsTarget then local tp = Players:FindFirstChild(states.statsTarget); if tp then local ls = tp:FindFirstChild("leaderstats"); for s, label in pairs(statsDisp) do local rawVal, isBool = 0, false; if s == "Size" then rawVal = tp:FindFirstChild("customSize") and tp.customSize.Value or 1 elseif s == "Speed" then rawVal = tp:FindFirstChild("customSpeed") and tp.customSpeed.Value or (tp.Character and tp.Character:FindFirstChild("Humanoid") and tp.Character.Humanoid.WalkSpeed or 16) elseif s == "Kill" then rawVal = ls and ls:FindFirstChild("Kills") and ls.Kills.Value or 0 elseif s == "Brawl" then rawVal = ls and ls:FindFirstChild("Brawls") and ls.Brawls.Value or 0 elseif s == "Brawl Kill" then rawVal = tp:FindFirstChild("brawlKills") and tp.brawlKills.Value or 0 elseif s == "Evil Karma" then rawVal = tp:FindFirstChild("evilKarma") and tp.evilKarma.Value or 0 elseif s == "Good Karma" then rawVal = tp:FindFirstChild("goodKarma") and tp.goodKarma.Value or 0 elseif s == "Muscle King Time" then rawVal = tp:FindFirstChild("muscleKingTime") and tp.muscleKingTime.Value or 0 elseif s == "Rebirth Multiplier" then rawVal = tp:FindFirstChild("rebirthMultiplier") and tp.rebirthMultiplier.Value or 0 elseif s == "Auto Lift" then rawVal = tp:FindFirstChild("autoLiftEnabled") and tp.autoLiftEnabled.Value or false; isBool = true elseif s == "Save Size" then rawVal = tp:FindFirstChild("saveSizePlayerValue") and tp.saveSizePlayerValue.Value or false; isBool = true elseif s == "Save Speed" then rawVal = tp:FindFirstChild("saveSpeedPlayerValue") and tp.saveSpeedPlayerValue.Value or false; isBool = true elseif s == "Current Map" then rawVal = tp:FindFirstChild("currentMap") and tp.currentMap.Value or "None"; isBool = true elseif s == "Rebirth" then rawVal = ls and ls:FindFirstChild("Rebirths") and ls.Rebirths.Value or 0 else if ls and ls:FindFirstChild(s) then rawVal = ls[s].Value elseif tp:FindFirstChild(s) then rawVal = tp[s].Value end end; if not isBool then local current = tonumber(rawVal) or 0; if lastStatValues[s] and current > lastStatValues[s] then statGains[s] = current - lastStatValues[s]; statGainTimes[s] = os.time() end; lastStatValues[s] = current; local gainStr = ""; if statGains[s] and statGainTimes[s] then if os.time() - statGainTimes[s] <= 2 then gainStr = " (+ " .. formatNum(statGains[s]) .. " Per Train)" else statGains[s] = nil; statGainTimes[s] = nil end end; label.Text = s .. ": " .. formatNum(current) .. (s == "Rebirth Multiplier" and "%" or "") .. gainStr else label.Text = s .. ": " .. tostring(rawVal) end end; if states.currentPetLabel then local petList = petRefsBasic; if states.currentPetTable == "Basic" then petList = petRefsBasic elseif states.currentPetTable == "Advanced" then petList = petRefsAdv elseif states.currentPetTable == "Rare" then petList = petRefsRare elseif states.currentPetTable == "Epic" then petList = petRefsEpic elseif states.currentPetTable == "Unique" then petList = petRefsUnique end; handleItemSelection(states.currentPetLabel, petList, petDetails) end; if states.currentAuraLabel then local auraList = auraRefsBasic; if states.currentAuraTable == "Basic" then auraList = auraRefsBasic elseif states.currentAuraTable == "Advanced" then auraList = auraRefsAdv elseif states.currentAuraTable == "Rare" then auraList = auraRefsRare elseif states.currentAuraTable == "Epic" then auraList = auraRefsEpic elseif states.currentAuraTable == "Unique" then auraList = auraRefsUnique end; handleItemSelection(states.currentAuraLabel, auraList, auraDetails) end; local uStr, ultF = "", tp:FindFirstChild("ultimatesFolder"); if ultF then for _, u in ipairs(ultF:GetChildren()) do uStr = uStr .. u.Name .. ": " .. formatNum(u.Value) .. "\n" end end; ultList.Text = uStr == "" and "None" or uStr; local gpStr, opF = "", tp:FindFirstChild("ownedGamepasses"); if opF then for _, g in ipairs(opF:GetChildren()) do gpStr = gpStr .. g.Name .. "\n" end end; gpList.Text = gpStr == "" and "None" or gpStr else resetStatsUI() end else for s, label in pairs(statsDisp) do label.Text = s .. ": None" end end end end))
table.insert(getgenv().ML_Expert_Loops, task.spawn(function() while task.wait(1) do if states.stopEverything then continue end; local statsList, currentMap = {}, {}; for _, p in ipairs(Players:GetPlayers()) do local dName = p.DisplayName .. " (@" .. p.Name .. ")"; currentMap[dName] = p.Name; displayNameMap[p.Name] = p.DisplayName; table.insert(statsList, dName) end; nameMap = currentMap; statsPlayerDD.refresh(statsList) end end))
table.insert(getgenv().ML_Expert_Loops, task.spawn(function() while task.wait(600) do if states.stopEverything then continue end; pcall(function() VirtualInputManager:SendMouseButtonEvent(0, 0, 1, true, game, 0); task.wait(0.1); VirtualInputManager:SendMouseButtonEvent(0, 0, 1, false, game, 0) end) end end))
table.insert(getgenv().ML_Expert_Loops, task.spawn(function() while true do if states.lessLag then task.wait(0.1) else RunService.Heartbeat:Wait() end; if states.stopEverything then continue end; local char = player.Character; if not char then continue end; local p = getPunch(); if p and p:FindFirstChild("attackTime") then if states.fastPunch then pcall(function() p.attackTime.Value = 0 end) elseif not states.autoGlitch then pcall(function() p.attackTime.Value = 0.35 end) end end; if states.autoPunch and not states.isTransitioning then local equippedTool = char:FindFirstChildOfClass("Tool"); if equippedTool and equippedTool.Name == "Punch" then pcall(function() equippedTool:Activate() end) end; if states.accelReps then local ult = player:FindFirstChild("ultimatesFolder") and player.ultimatesFolder:FindFirstChild("+5% Rep Speed"); if ult and ult.Value ~= 1000 then pcall(function() ult.Value = 1000 end) end end end end))
SetToggle[ui.walkOnWaterBtn](true)

local function onUnload()
for _, list in pairs(disabledAttachments) do for _, inst in ipairs(list) do if inst.Parent then pcall(function() inst.Enabled = true end) end end; table.clear(disabledAttachments)
for _, list in pairs(anchoredParts) do for _, part in ipairs(list) do if part.Parent then pcall(function() part.Anchored = false end) end end; table.clear(anchoredParts)
for targetName, size in pairs(originalTorsoSize) do local targetPlayer = Players:FindFirstChild(targetName); if targetPlayer and targetPlayer.Character then local t = findTorso(targetPlayer.Character); if t then pcall(function() t.Size = size end) end end; table.clear(originalTorsoSize)
for item, conns in pairs(activeConnections) do for _, conn in ipairs(conns) do pcall(function() conn:Disconnect() end) end end; table.clear(activeConnections)
for folder, conns in pairs(activeWatchers) do for _, conn in ipairs(conns) do pcall(function() conn:Disconnect() end) end end; table.clear(activeWatchers)
for _, loop in ipairs(getgenv().ML_Expert_Loops or {}) do if type(loop) == "thread" then pcall(task.cancel, loop) elseif type(loop) == "table" and loop.Disconnect then pcall(loop.Disconnect, loop) end end
if getgenv().ML_Expert_UI then pcall(function() getgenv().ML_Expert_UI:Destroy() end) end
if states.flyBody then if states.flyBody.Velocity then states.flyBody.Velocity:Destroy() end; if states.flyBody.Gyro then states.flyBody.Gyro:Destroy() end end
if walkOnWaterPlatform then walkOnWaterPlatform:Destroy() end
for rock, data in pairs(rockCache) do if rock and rock.Parent then rock.Size = data.Size; rock.CanCollide = data.CanCollide; rock.CFrame = data.CFrame; rock.Anchored = data.Anchored end end
end
task.spawn(function() while true do task.wait(1); if not getgenv().ML_Expert_UI or not getgenv().ML_Expert_UI.Parent then onUnload(); break end end end)
